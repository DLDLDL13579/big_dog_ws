# 机械狗项目运维与状态文档

> 最后整理：**2026-09-24**（新增§「★ 2026-09-24 开机自启（导航+协同）+ 初始位姿时序修复」）
> 上次整理：2026-09-23（新增§「★ 2026-09-23 列队容差与网络变更」）
> 上次整理：2026-09-21（新增§「★ 2026-09-21 控制器·死区·横移 变更」；更新「启动导航后必查」的初始位姿与 vy 判据）
> 上次整理：2026-09-15（删除两份被取代的状态快照，保留轮次总览与运维检查项）

## ★ 2026-09-24 开机自启（导航+协同）+ 初始位姿时序修复（最高优先级）

> 本节为 2026-09-24 部署「开机即完整导航 + 多机协同」后的权威状态。涉及 **systemd 服务
> 布局、linger、auto_initial_pose 时序**三块。源码改动见提交 `4940852`。

### 一、目标与最终服务布局

**目标**：狗开机后自动进入**完整导航**并接入**多机协同**，无需人工干预。

| 服务 | 级别 | 作用 | enabled | 备注 |
|---|---|---|---|---|
| **`dog-nav`** | **user** | 导航主栈（`bringup_launch.py mode:=navigation`） | ✅ **enabled** | **新增**，替代手动 `nav_restart.sh` |
| **`robot2-adapter`** | **user** | 多机协同上行适配器 | ✅ enabled | 已加 `After=dog-nav.service` |
| `dog-brain` | system | 建图模式（`mode:=mapping`） | ❌ **disabled** | 按用户要求关闭自启，**保留手动启动能力** |
| **`linger`** | — | 让 user 服务在未登录时也开机自启 | ✅ **`Linger=yes`** | user 级方案的关键开关 |

**为何用 user 级而非 system 级**：ROS 工作流本就是用户态（`dog_ws` 在 `~` 下、`~/.bashrc`
配域、`nav_restart.sh` 以 nvidia 跑）；user 级无需 sudo、环境更接近手动操作、避免 sudo
重置环境变量。唯一代价是需 `loginctl enable-linger nvidia`（已开启）。

**跨级不互斥的注意点**：`dog-brain`（system）与 `dog-nav`（user）**级别不同，systemd
`Conflicts=` 无法跨级生效**。故靠 ①`dog-brain` 已 disabled（开机不跑）避免冲突。若日后
手动建图，请先停导航：
```bash
systemctl --user stop dog-nav && sudo systemctl start dog-brain
```

### 二、服务文件与部署

**单元文件位置**（板端）：
- `~/.config/systemd/user/dog-nav.service`
- `~/.config/systemd/user/robot2-adapter.service`

**Mac 侧留档**：`机械狗研发资料/systemd服务留档/`（含两个 `.service` + 一键部署脚本
`deploy_dog_autostart.sh`）

**`dog-nav.service` 关键配置**（每项都对应一个已踩过的坑）：

| 配置 | 值 | 为什么 |
|---|---|---|
| `ROS_DOMAIN_ID` | `11` | 狗的域，与主车(1)/小车1(21)隔离 |
| `FASTRTPS_DEFAULT_PROFILES_FILE` | `~/fastdds_udp.xml` | **R24 事故**：FastDDS SHM 静默死亡（进程存活但 Publisher count=0）→ 强制 UDP |
| `PATH` / `PYTHONPATH` | 显式写死、**剔除 conda** | **致命**：conda 的 python 缺 `numpy`/`tf_transformations`，会让定位四件套（`global_map_publisher`/`pcd_to_map`/`global_localization`/`transform_fusion`/`elevation_map`）**全部崩溃** |
| `ExecStartPre` | `/bin/sleep 12` | 等网络/雷达就绪，否则 FAST-LIO 失同步 |
| `ExecStart` | `mode:=navigation` + `auto_initial_pose:=true` + **H 点坐标** | 显式传发车点 `(0.000, -0.111, yaw -0.046)`，否则 ICP 永不初始化 |
| `enable_camera` | `false` | 相机禁用是省 CPU 的主动选择 |
| `Restart` / `RestartSec` | `on-failure` / `8` | 崩溃自愈 |
| `StartLimitBurst` / `StartLimitIntervalSec` | `3` / `120` | 地图损坏时防止无限重启刷日志 |

**日常运维命令**（user 级必须带 `--user`）：
```bash
systemctl --user status  dog-nav robot2-adapter
systemctl --user restart dog-nav              # 重启导航栈
journalctl --user -u dog-nav -f               # 看导航日志
journalctl --user -u robot2-adapter -f        # 看协同日志
```

### 三、★ 初始位姿时序修复（`4940852`）

**问题**：开机后 ICP 一直 `Waiting for global map`，**不会自动定位**，需人工补发
`initialpose` 才触发。

**根因**（实测时序，16:48:58 启动）：
```
16:49:01  /Odometry received
16:49:01  Waiting 8.0s for global map...     ← --map-grace 默认仅 8s
16:49:12  发布 initialpose                    ← 此时 ICP 尚未收到地图
16:50:06  Global map received                 ← 晚 54s ❌
```
`global_map_publisher` 的发布间隔是 **60s**（2026-09-14 为降流量从 5s 改为 60s，其注释
称「60 秒窗口足以覆盖 ICP 启动」——**该假设不成立**），而 `publish_initial_pose.py` 的
`--map-grace` 默认仅 **8s**，覆盖不到一个发布周期。

**修法**（最小改动，**不改 Python 源码**）：`navigation_launch.py` 里给
`publish_initial_pose.py` 显式传 `--map-grace 70.0`（> 60s 发布周期）。

**为何不改发布间隔/QoS**：ICP 端 `cb_init_map` 收到首帧即 `destroy_subscription`，
只需保证「发布时 ICP 已订阅」；改发布频率会破坏 09-14 的降流量优化，改 QoS 需同步改
ICP 订阅端（源码注释已明确警告）。

**修复后实测**（16:53:39 启动）：
```
16:53:41  Waiting 70.0s for global map...
16:54:46  Global map received      ← 先到
16:54:54  Publishing initial pose  ← 后发
16:54:55  ICP refine fitness       ← 自动匹配成功 ✓
```
**开机后 ICP 可自动初始化，无需人工干预。**

### 四、验证方法与结果（2026-09-24 实测）

| 检查项 | 方法 | 结果 |
|---|---|---|
| 开机自启开关 | `systemctl is-enabled` | dog-nav=enabled / robot2-adapter=enabled / dog-brain=disabled ✓ |
| **未登录也能自启** | 在 `0 users`（无登录会话）状态下 stop→start | 两个服务均正常拉起 ✓（linger 生效） |
| 导航栈完整性 | `ros2 topic list` | 话题 **18 → 73**；`/Odometry` `/map` `/map_to_odom` `/localization` `/cmd_vel` `/global_costmap/costmap_raw` `/robot_2/amcl_pose` 全在 ✓ |
| Nav2 激活 | 日志 | lifecycle_manager 激活 10 个服务器 ✓ |
| 地图加载 | 日志 | `Converted map to PointCloud2, 1624173 points` ✓ |
| ICP 自动触发 | 日志 | `ICP refine fitness` 有输出 ✓ |
| 导航栈报错 | `journalctl` 错误计数 | 0 ✓ |

**已知良性现象**：手动 `systemctl --user stop robot2-adapter` 时会被记为 `failed`
（rclpy 收到 SIGTERM 抛 `ExternalShutdownException`，退出码 1）。**不影响开机自启**
（自启走 `start` 不经 stop），且 `Restart=always` 会 3 秒内自动拉回。若要去掉该现象，
可加 `KillSignal=SIGINT`（可选优化，未做）。

### 五、★ 使用前提：狗必须在地图覆盖区内

**服务层就绪 ≠ 导航可用**。ICP 是**局部优化**（从 `initialpose` 初值 refine），
**若狗不在建图覆盖区**：
- ICP 仍可能收敛到"数学上最优"的位姿（fitness 可能很高，实测 0.987~0.990）
- **但该位姿物理上是错的（假定位）**——发目标会导错方向

**判据不是 fitness 高低，而是「狗是否真的在地图覆盖区内」**。

当前地图（`lab_3d_map.pcd`，2026-09-18 建）：1380×910 @0.05m，origin `(-12.812, -15.218)`，
自由区覆盖约 `x: -11.81~27.19m`、`y: -11.77~26.23m`；地图 **68.66% 为未知区**。

**若需在新区域导航**：跑 `mapping_launch.py` 建图 → 存图 → 替换
`src/dog_brain/maps/lab_3d_map.pcd`（`nav_restart.sh`/`dog-nav` 均不传 `map_pcd` 参数，
导航只认这个文件本身）。

---

## ★ 2026-09-23 列队容差与网络变更（最高优先级）

> 本节为 2026-09-23 实车调试后的权威状态。涉及 **Nav2 目标容差、列队间距、在线探测网段、
> 双网段 DDS 白名单**四块。详细推导见《技术架构与调参》§「★ 2026-09-23 列队精度容差 订正」。

### 一、Nav2 目标容差（列队精度相关）
- `general_goal_checker.xy_goal_tolerance`: 0.40 → **0.25 m**
- `general_goal_checker.yaw_goal_tolerance`: 0.3 → **0.20 rad（11.5°）**
- `FollowPath.xy_goal_tolerance` / `yaw_goal_tolerance` 同步（L215/L216）
- 过程：0.40 → 0.20（过严，狗精调打转不收敛）→ 回调 **0.25**
- 依据：**主车板** `~/bags/formation_test_20260923`（旧值，28 MB）、`~/bags/formation_test2_20260923`（新值对比，141 MB） ←订正2026-09-24：两 bag 录制于**主车**（`nvidia@192.168.31.43`），非狗板；实测两车到位 0.152/0.135 m

### 二、双网段 DDS 白名单（换网不用改配置）
- `~/fastdds_udp.xml` 的 `interfaceWhiteList` 由单地址改为**双地址**：
  `172.20.10.3`（iPhone 热点）+ `192.168.31.91`（小米 WiFi）
- 原因：2026-09-23 换到小米 WiFi 后，单地址白名单导致
  `All whitelist interfaces were filtered out` → DDS 参与者为空 → 话题表只剩 2 条
- 备份：`fastdds_udp.xml.bak_xiaomi_20260923`

### 三、主车侧配套变更（同步记录，便于跨机排查）
- 在线探测改为**双网段列表**（任一 IP 的 TCP:22 可达即在线）：
  `robot_1: [192.168.31.235, 172.20.10.2]`、`robot_2: [192.168.31.91, 172.20.10.3]`
- `PROBE_TIMEOUT` 1.0 → **3.0 s**（热点网 RTT 高，狗 SSH 握手实测 2.36 s，1 s 必误判离线）

## ★ 2026-09-21 控制器·死区·横移 变更（最高优先级）

> 本节为 2026-09-21 实车调试后的权威状态。涉及**控制器架构、UpBoard 死区、转向抬底、
> 横移通路、初始位姿**五块。详细依据与源码级推导见《技术架构与调参》§「★ 2026-09-21 控制器·死区·横移 订正」。

### 一、控制器：改为 RotationShim 包裹 DWB
- `FollowPath.plugin` = **`nav2_rotation_shim_controller::RotationShimController`**（原 `dwb_core::DWBLocalPlanner`）
- `primary_controller` = `"dwb_core::DWBLocalPlanner"`（**字符串参数**，DWB 参数仍平铺 `FollowPath.*`）
- 起因：路径首段与狗朝向夹角中位 134.4°、|差|>90° 占 84.5%，狗只能倒车磨，绕路 5.37 倍
- **★ 关键修复（死锁）**：`closed_loop=false`（原 true）+ `max_angular_accel=4.0`（原 1.5）
  - 原因：`dt=1/10Hz=0.1s`，原值把 feasible 区间压成 `current±0.15`，期望的 ±0.35 被钳到 ±0.15，
    而 ±0.15 在无效区 → 狗转不动 → `current` 恒 0 → **永久死锁**（实测 RotationShim 输出恒 −0.1500）

### 二、UpBoard 死区：0.075 → 0.03（导航档，二进制补丁）
- 实现：**二进制立即数补丁**（文件偏移 `0x4733d`，4 字节 `9a99993d`→`8fc2f53c`），**非重编译**
  - 原因：重编译产出的 `libbiomimetics.so` 与厂家版**C++ ABI 不兼容**，部署后 `mit_ctrl` 段错误崩溃
- 库 MD5：`a9594fb0…` → **`a0fbf3f5b97f746b5d6e68018c732276`**（大小不变 126998520）
- 回滚：`cp libbiomimetics.so.bak_075_keep_0921 libbiomimetics.so` + 重启 mit_ctrl
- **注意**：二进制补丁全库改值，**遥控档死区也变**（未做分流）

### 三、转向抬底 `NAV_W_FLOOR`：0.19 → 0.31
- **0.19 实测是「死区谷底」**：桥接 0.19 → 实测 wz **−0.037（符号反向）**
- 0.31 实测 +0.2003 有效；改后桥接 wz 有 80% 落在有效区
- 症状（改前）：狗卡在距目标 0.28m 处 26 秒原地转（wz 恒 −0.190）→ `Failed to make progress` → 超时

### 四、横移（vy）已贯通
| 环节 | 值 |
|---|---|
| UpBoard TCP | **4×double `[flag, ω, v, vy]`**（原 3×double） |
| UpBoard 导航档 | `v_des[1] = buf_socket_data[3]`（原硬写 0；放在 `if(tcp_fresh)` 内，断线清零） |
| `librobot.so` | **`7fbefc5d86ee8047e94924face30ef12`**（原 `70209218…`）——含横移注入 + **recv 循环读满整帧修复** |
| bridge | `NAV_Y_CHAIN=2.0` / `NAV_Y_MAX=0.30` / `NAV_Y_ACCEL=0.5`；`pack('<4d', …, tcp_vy)` |
| Nav2 | `min_vel_y=-0.25` / `max_vel_y=0.25` / `acc_lim_y=0.5` / `decel_lim_y=-0.5` / `vy_samples=7` |
| velocity_smoother | `max_velocity=[0.45, **0.25**, 1.5]`（三元组 `[vx,vy,wz]`，**vy 位为 0 会钳掉横移**） |

- **实测**：`linear.y=0.40` 持续 2s → UpBoard 日志 `横移:0.200` → 狗**左移 0.37m**（`dy=+0.370`，符号正确）
- **recv 缺陷修复**：原单次 `recv` 未校验收满整帧，实测出现 `recvbyte:16`/`recvbyte:1` 帧错位；
  改为 `while(got<need)` 循环后累计 **21 万条 `recvbyte:32`，100% 完整**

### 五、导航初始位姿：改为机械狗发车点（自动）
- `navigation_launch.py` 默认值：`init_x=0.000` / `init_y=-0.111` / `init_yaw=-0.046`（发车点 H）
- `bringup_launch.py` **新增声明并转发** `auto_initial_pose`/`init_x/y/z/yaw`（原只转发 `enable_camera`）
- `nav_restart.sh` 显式传参
- **实测**：重启后日志 `auto_initial_pose: Publishing initial pose: x=0.0, y=-0.111, z=0.0, roll=0.0, pitch=0.0, yaw=-0.046` ✅
- ⚠️ **狗不在发车点时需手动补发 `/initialpose`**（自动发布的只是初值，ICP 会收敛，但初值偏差过大可能失败）

### 六、2026-09-21 实车结果
- `nav_manual_014`：用户在 RViz 手动发 **4 个远距离目标**（间距 8.10 / 9.02 / 13.85 m）→ **4/4 全部到达**，
  绕路 **1.26~2.56 倍**，最远目标 **(+10.76, −0.03)**，含一次**避障成功**（用户站于狗前）
- **场地尺度订正**：此前推断的"可通行 2.6m×2.7m"**有误**（那只是历史导航点覆盖范围）；
  实测**场地至少 11m×9m 可通行**

### 七、工程规约（用户 2026-09-21 明确）
- **只改 `src`，绝不直接改 `build/`、`install/` 的代码**；保证 src 改完后**已构建或为软链接**
- 本工作区 `install/dog_brain/share/.../{config,launch}` 与 `build/lcm_bridge/lcm_bridge` **均为软链接→src**
- `lcm_bridge` 为 editable install，**实际加载 `build/lcm_bridge/lcm_bridge/bridge_node.py`**（软链接→src）
- **教训**：`/cmd_vel_bridge` 是**监控话题**，其 `linear.y` 从未被填充 → 曾据此误判「横移被丢弃」。
  **判断指令是否下发必须看接收端（UpBoard 日志），不能看中间监控话题**

---

## ★ 多机协同接入（2026-09-09 新增，最高优先级认知）

> 狗已加入三车协同体系：主车(域1 调度中枢) / 小车1 robot1(域21) / 狗 robot2(域11)。本节覆盖旧的"单车"认知。


### 0910 更新：联网形态定型 + 新有墙地图
- 联网：iPhone 热点静态 172.20.10.2/28（第一优先级）＞小米（第二）；robot2-wifi-roam.timer 30 秒漫游；**省电模式已关并持久化**（wifi-powersave-off.conf）——反复掉线根因终结
- 建图：三轮产物中**采用 18:24 版**（21MB；闭环路程 110m、净位移 0.5m）；15:00 的 39.9MB 版因漂移鬼影污染弃用（归档 `lab_3d_map_replaced_20260910.pcd`）←订正2026-09-14。**2D 图 09-14 导出并下发主车/小车1**：`dog_map_20260910`（1364×926 @0.05m，origin -15.10,-15.10）。墙体"虚线化"= **实验室玻璃墙**（激光穿玻璃只偶尔返回，用户 09-14 澄清），非采样密度不足，不重扫
  - **←订正2026-09-20：09-10 那版图已于 2026-09-18 被重新建图取代，上句的 21MB / 1364×926 / origin -15.10,-15.10 均已作废。** 现况：在用地图仍为同一路径 `src/dog_brain/maps/lab_3d_map.pcd`，内容换为 09-18 新图（**51973789 字节≈52MB，1624173 点**；走行约 120m 闭合回环、终点闭合误差 0.0m、Z 漂移 0.003m），旧图备份 `lab_3d_map.pcd.bak_20260910`。**2D 图为 1380×910 @0.05m，origin −12.812,−15.218**，已与主车在用融合图对齐（实测差 <0.001m）。换图原因：旧图坐标系与主车侧不一致，同一地图坐标在两车上指向不同现实位置
- dog-brain 服务已补 UDP profile 环境（曾缺配置复现 R22 SHM 静默丢点云——已修）
- 基线包：~/nav_bag_910_0910_1840（888 秒/12 话题/轨迹 18.9×13.0m/定位修正量恒定）

### 狗侧协同组件（已入库）
| 组件 | 位置 | 说明 |
|---|---|---|
| `robot2_adapter.py` | 狗 `~/robot2_adapter.py`（副本在本仓库） | **话题适配器**：/Odometry→/robot_2/{odom,soldier_pose,amcl_pose}（10Hz，帧改 robot_2/*）；/map→/robot_2/map；/plan→/robot_2/plan；/tf→/robot_2/tf_relay（仅 map→odom→base_link）；下行 /robot_2/{goal_pose,initialpose}→/goal_pose、/initialpose；/robot_2/cmd_vel→/cmd_vel（**远端 MQTT 遥控已实测走通**）|
| `robot2-adapter.service` | 狗 systemd user 单元（副本在本仓库） | 常驻适配器；ROS_DOMAIN_ID=11 + UDP profile |

### 关键事实
- 狗真实话题名与协同约定不符是最大坑：真实狗发 /Odometry /map /plan（裸帧名 odom/base_link），主车桥订阅 /robot_2/*——**适配器是必须的**
- 狗**无电池话题**（上行 battery 恒 null，远端显示无电量属预期）；无 2D scan（livox 3D）
- **整机重启流程**：①（登录后）适配器自动/手动拉起 ②`bash ~/nav_restart.sh` 启动导航 ③主车侧桥**零操作**（--auto-remove 自动重建，已真机断电复验）
- ⚠️ 适配器为 user 级 systemd 但**开机不自启**（等登录），待办 `sudo loginctl enable-linger nvidia`
- ⚠️ **WiFi 空口雷区**：远端控制台开启 → 主车媒体流 ~2.8MB/s 挤垮 2.4G → 狗（链路最弱端）掉线/丢指令；狗尽量停信号好处
- MQTT 状态三层：online=TCP:22 探测 / nav_running=odom 新鲜度 / pose>5s null（主车 system_manager 2026-09-09 晚改版，字段见主车仓库《06_robots字段说明》）
- 排查教训：判链路真假勿信 `ros2 topic hz`（daemon 缓存误报）；测下行禁用 `--once` 短命发布者（必丢，用长驻或连发）；栈重启过渡期 TF 残留旧值，判断需带新鲜度校验

---

## ★ 轮次总览 R15→R27（Mac 工作区整理 2026-09-11；R27 于 09-15 追加）

> **本节为 Mac 工作区补充整理，非设备原文。** 目的：R 轮记录原本分散在两处
> （R1~R21 在《技术架构与调参》附录 E、R22~R25 在本文档下方），此处给出一张**统一索引表**，
> 便于快速定位"哪一轮改了什么、结论是什么"。逐轮细节仍以对应章节原文为准。
> 数据来源：狗板 `git log`（53 commits，HEAD `2bfcd8c`）+ 两份文档原文，逐条核对。

### 控制层（lcm_bridge 指令链）

| 轮次 | 日期 | 改了什么 | 实车结论 / 遗留 |
|---|---|---|---|
| **R9** | 08-28~31 | bridge 死区**只抬底不放大**（废除 [0.01,0.75]→[0.24,0.75] 拉升）；倒向 ÷1.5 补偿；DWB 参数对齐（min_vel_x / xy 容差 0.40 / 膨胀） | ✅ 阶梯①②通过：稳态恒等 ±3~6%、倒向全活（比例 0.99）、超速消除（0.45→0.465）；阶梯③因雷达掉线作废 |
| **R10** | 08-31 | 标定脚本三处修复（TCP 预检 / NameError / shutdown 死锁）；robot2-soldier 停止（清幽灵 /cmd_vel） | 运动延迟 0.3~0.75s、停车滑行 0.08~0.29m → 支撑 `xy_goal_tolerance=0.40` |
| **R11** | 09-01 | bridge **减速直通锁存**（减速坡尾 <NAV_V_MIN 不再抬成 0.24 平台） | ⚠ R11-4 撞墙教训：**发车必须用户明确说"发车"，预告不等于批准** |
| **R12** | 09-01 | wz 转向抬底可开关（`enable_wz_floor` 默认 True，`wz_floor=0.19`）；D435i 相机默认关闭 | L4-4 单变量 -30° 从 FAIL 翻 PASS |
| **R13** | 09-02 | **锁存驻留解锁**（`enable_latch_dwell` 默认 True，dwell=2.0s）：锁存中 vx 同值驻留 >2s 自动释放+抬底 | 消除走走停停死锁（L5-4/6/7 实证）；rearm 机制防 R9 平台回归 |
| **R14** | 09-02 | 加 `RotateToGoal` critic（scale=24）+ `trans_stopped_velocity=0.25` + `slowing_factor=5.0` | 位置过冲改善（0.03m 历史最佳）；yaw 收敛依赖触发时机（3 轮：1 成功 / 2 超时） |
| **R15** | 09-02~03 | **wz 方向锁定**（锁定首个非零方向，反向需持续 ≥lock_sec 才切换），lock_sec 2.0→**5.0** | R15-1 直线 ✅（1m，误差 0.089m，wz 仅 1 flip）；R15-2 斜线+90° ❌（y 误差 0.705m，yaw 只到 55.2°）→ 暴露"锁太强 + 强制反向执行" |
| **R16** | 09-03 | lock_sec 5.0→**1.0** + 反向**置零**（废除 copysign 强制同向）；双条件解锁（强修正 ≥0.30 持续 0.3s / 反向持续 lock_sec） | 实锤 R15 反向失误：方向一致率仅 56.5%、G6 终点反向 177.6°；但"冻结 15.3%"代价过大 |
| **R17** | 09-03 | lock_sec 1.0→**0.3** + 反向**衰减 30%**（不再置零） | 锁切换间隔实测 1.3~2.2s；**发现处理顺序问题**：先抬底会把微小符号抖动放大成方向切换事件 |
| **R18** | 09-03 | **抬底移到方向锁之后**（只对"确认执行的方向"抬底，反向被衰减的不抬）——**当前 bridge 基线** | 一次性测试 7 场景全 PASS |
| **R19** | 09-04 | 静态可靠性 5 项：TCP `sendall` 定长帧 / 导航模式禁用 `/Laser_map` 累积与存图 / Odometry covariance 发布顺序 / `/initialpose` 按 SE(2) 换算 / ICP 用 `read_points_numpy` + 径向 FOV 裁剪 | 全部为"可证明正确"的修复，**不改 R18 控制参数** |
| **R20** | 09-04 | `SimpleProgressChecker`→**`PoseProgressChecker`**（+`required_movement_angle=0.15`）；FAST-LIO 填 `/Odometry.twist`；Spin 限 0.19~0.35 rad/s、角加速度 1.0；新增 `/cmd_vel_bridge` 诊断话题 | progress failure **11→0**；但暴露：`/Odometry` 延迟中位 1.347s、60.47% 控制样本含 DWB 不允许的 `vy`、34 次无有效轨迹中 33 次伴随模型不兼容 → 直接喂 DWB 方案终止 |
| **R21** | 09-04 | **新增 `/nav_odom`**（桥接层合成：vx=最终桥接目标、vy 恒 0、wz=UpBoard IMU 低通），**仅** `controller_server.odom_topic` 改读它 | R23 满电复测验证有效：DWB 输入越界 0%、`No valid trajectories` 34→0 |
| **R22** | 09-07 | **livox 主机时间戳补丁**（`lddc.cpp`；设备钟落后主机 3.0s 且漂移 6~15s/h）；定位"FAST-LIO 稳态队列积压 1.45s"根因；新增发车门禁与测量脚本 | `/livox/lidar` 1.09s→**10.0ms**、`/livox/imu`→0.6ms、`not Synced`/`loop back` 归零 |
| **R23** | 09-07 | R21 **满电复测** | ✅ **项目史最佳**：`/Odometry` 中位 25.4ms、9 随机目标 **78%**（7成/2抢/0abort）、越界/extrapolation/oob 全 0。⚠ 撤回"电池是主要混淆因素"（Jetson 两轮均未降频），增益归因**挂起** |
| **R24** | 09-07 | **FastDDS SHM 静默死亡**事故复盘 + UDP-only 加固（`fastdds_udp.xml`，禁 SHM + 8MB 缓冲） | 症状：进程存活但输出话题 Publisher count=0。修复后 `/Odometry` P99 28.4ms、超 0.1s 占比 **0.00%**、漂移 +0.012 s/h。跨机通信无影响 |
| **R25** | 09-07 | **2D 平地导航封板**：冻结基线 + 固化清单 + 已知限制 | 冻结基线：`/Odometry` 中位 ~24ms、`/nav_odom` 年龄 ~0.5ms 且 vy 恒 0、各类越界/abort 全 0、成功率 50~78%。**后续改动需走回归** |
| **R26** | 09-10 | 接入三车协同：话题适配器入库、多机协同运维节、下行 cmd_vel 远端遥控实测、在线三层判定 | 狗以域 11 接入；适配器为必需件（真实话题名与协同约定不符） |
| **R26b** | 09-10 | 联网形态定型（热点静态 172.20.10.2 + 省电根因 + 漫游 timer）+ 三轮建图定稿：采用 18:24 版（21MB），15:00 的 39.9MB 版因鬼影污染弃用 ←订正09-14 | 掉线循环终结；**2D 图 09-14 导出并下发主车/robot1** |
| **R27** | 09-15 | **2.5D 阶段① 实机验证 + 楼梯可通行区**：`elevation_map`→`global_costmap.elevation_layer` 链路验证；修复初始位姿时序竞态；导航实车闭环；楼梯 bbox 标定；可通行区方案落地 | 实机与离线复算**四项吻合**；导航 **SUCCEEDED** 实走 3.27m；坑与方案详见《2.5D-3D进阶方案》§10.7/§11 |

### 阶段小结（截至 2026-09-15）

- **已封板**：FAST-LIO 定位 → Nav2 规划控制 → bridge/TCP → UpBoard RL 的**平地导航链路**（R25，09-07）。
- **已接入**：三车协同（域 1/21/11）、新有墙地图、联网形态定型（R26/R26b，09-10）。
- **进行中**：**2.5D 可通行性**（R27，09-15）——配置已实机验证通过、导航闭环打通、楼梯已标定；可通行区改动**代码就绪、待上机**（《2.5D-3D进阶方案》§11）。
- **未完成 / 挂起**：
  1. **执行侧转向增益归因**（实测 0.45~0.87 随电量/温度波动）——用户已决定不做电压/温度监控，归因挂起。
  2. **2.5D 后续**：可通行区上机验证 → 控制层地形限速 → 定位层 z 归零条件化（这是"狗真能爬上去"的瓶颈）。
  3. 失同步不自愈、`dog-nav.service` 未建、适配器 `linger` 未开。
- **D435i 现状（2026-09-11 用户确认）**：相机**硬件可用**；当前三处相机消费方全部禁用是**主动省 CPU** 的选择，
  **不是硬件故障**：
  - `navigation_launch.py` 注释掉 `elevation_map`（CPU 57%）与 `step_detector`（CPU 56.9%）两个节点
  - `nav2_3d_params.yaml` 的 `observation_sources` 只有 `livox_scan`；`camera_scan` 行内注释写"08-27 D435i 硬件故障"，
    那是**当日的历史原因**，已不适用于现在
  - `enable_camera` 默认 `false`
  - 恢复路径（属上条"2.5D 可通行性感知"的前置）：`enable_camera:=true` → 取消两节点注释 → 标定 `step_max=0.50`；
    恢复前需权衡 CPU 预算（当前导航链路已封板，`control_loop_missed` <1.1/min）
- **UpBoard [NAV-TCP] 补丁（2026-09-11 实机核对）**：TCP 导航注入**已部署并经 R10→R25 实车验证**，
  但补丁源码**不在厂家发布包内**，存在"重装即丢失"风险：
  - 补丁源码工作区 `~/bigdog_build/XJZX-bigdog/`（08-18 打补丁）｜编译目录 `~/bigdog_build/build_nosim/`
  - 运行产物 `robot-software/build/librobot.so`（MD5 `702092187f9c766ee8f739920f2eca02`，Aug 19 14:06）
  - 补丁 = SWF 双源切换 + **300ms 看门狗自动停车** + 接收线程互斥锁/时戳 + `recvbytes<=0` 边界
  - ⚠ 发布包里的 `rt_rc_interface.cpp` 是 TCP 被注释的版本（MD5 `e5c9458d…`），与补丁版（`5ade5bf1…`）同名不同内容
  - 自检：`strings -a robot-software/build/librobot.so | grep buf_socket_last_us`
  - 已在 Mac 侧留档 `机械狗研发资料/upboard补丁留档/`（源码 + MD5 + 重编/回退步骤）；详见《技术架构与调参》§2.3b
- **Git 推送能力（2026-09-11 配置完成）**：狗已可**直接在仓库里提交并推送**，无需 Mac 中转
  - 用法：`cd ~/dog_ws && git add ... && git commit -m "..." && git push origin main`
  - **网络事实**：`github.com` 的 **HTTPS/TLS 层被阻断**（TCP 可达但 TLS 挂），
    但 **SSH 通道完全可用**——`github.com:22` 与 `ssh.github.com:443` 均能完成握手
  - **授权方式**：仓库级 **Deploy Key**（`read_only: false`，ID `162964280`，仅对 `big_dog_ws` 有效）
    → 管理/撤销：repo → Settings → Deploy keys
  - 已配置：专用 ed25519 key `~/.ssh/id_ed25519`（指纹 `SHA256:VUpDU8TDPYO5Dj/+EMwP745ZJmbuNLeDVxsemlskIs8`）
    + `~/.ssh/config`（github.com 指定该 key）+ `origin` 切为 SSH
  - ⚠ 备用：若日后 `github.com:22` 被封，在 `~/.ssh/config` 的 `Host github.com` 段加 `Port 443`
  - 📌 教训：曾误判为"狗连不上 GitHub 必须走 Mac 中转"——实际是当时**设备网络波动**
    造成的假象；网络正常时 SSH 直连一直是通的

---


> 由 9 份手写文档整理合并（2026-08-27）。原件备份于 docs_backup_20260827/。
> 本文档三部分：① 多机协同接入 + 轮次总览 + 启动检查项；② 运维指南（原 OPS_GUIDE，含网络/建图/导航/systemd/故障速查）；③ 安全红线 + 任务清单 + 已知限制。

## ★ 启动导航后必查（运维检查项）

- `publish.map_en=false`、`pcd_save.pcd_save_en=false`、无 `/Laser_map` 累积
- ICP fitness 正常（应达到 1.000）、无重复 TF 发布者
- `/nav_odom` 新鲜；`/Odometry` 延迟应在 0.1s 量级。**注：`/nav_odom` 的 `vy` 仍恒 0**（该监控话题未回填 vy）——**横移是否真的下发要看 UpBoard 日志的 `横移:` 字段**，不能看 `/nav_odom` 或 `/cmd_vel_bridge` ←订正09-21
- **初始位姿已自动发布**（2026-09-21 起）：`nav_restart.sh` 传 `auto_initial_pose:=true`，默认发布机械狗发车点 `(0.000, −0.111, yaw −0.046)`。启动后查日志确认出现 `auto_initial_pose: Publishing initial pose: x=0.0, y=-0.111, ...`
  - **狗不在发车点时**需手动补发 `/initialpose`（自动发布的只是初值）
  - ICP 收敛判据：日志出现 `ICP refine fitness: 1.000`
  - 时序竞态细节见《2.5D-3D进阶方案》§10.7 坑 1  ←订正09-21
- **发车前提**：狗停在离障碍 0.6 m 以上的开阔处，否则脚下落进膨胀 lethal 区、规划器算不出路径
  —— 见同节坑 3

> **导航 ↔ 遥控切换规范（2026-08-28）**：导航中需要手动遥控时，**必须先 Cancel 当前导航 Goal**
> （RViz 点 Cancel 或 `ros2 action cancel`），确认 canceled/aborted 后再接管遥控；
> 遥控结束后重新导航时**发送全新 Goal**，不要续用旧 Goal。
> 原因：未 Cancel 就遥控，旧 Goal 的 `header.stamp` 会随 TF buffer 滚动而过期，
> 导致 `planner_server` 持续报 extrapolation into the past。

- **安全红线**：实车、UpBoard 或发车操作必须获得用户当轮明确授权。

---

## 运维指南（原 OPS_GUIDE.md）

> 原为独立的《机械狗项目运维指南》（OPS_GUIDE.md，2026-08-25 版）；现已并入本文档，
> 其参数值、状态一律以头部《★ 轮次总览》与设备代码为准。
> 适用对象：项目维护人员 / 后续 Agent。硬件：Jetson Orin NX（大脑）+ UpBoard（小脑）+ Livox Mid-360 + Intel D435i。

---

## 目录

1. [快速启动](#1-快速启动)
2. [网络配置与维护](#2-网络配置与维护)
3. [建图流程](#3-建图流程)
4. [导航流程](#4-导航流程)
5. [systemd 服务管理](#5-systemd-服务管理)
6. [日常运维脚本](#6-日常运维脚本)
7. [故障诊断与处置](#7-故障诊断与处置)
8. [代码修改与构建](#8-代码修改与构建)
9. [远程可视化 (RViz)](#9-远程可视化-rviz)
10. [UpBoard 端操作](#10-upboard-端操作)
11. [安全红线](#11-安全红线)
12. [多机协同与地图共享](#12-多机协同与地图共享)
13. [速查表](#13-速查表)

---

## 1. 快速启动

### 1.1 环境准备

每个新终端必须先 source：

```bash
source /opt/ros/humble/setup.bash
source /home/nvidia/dog_ws/install/setup.bash
```

> - `ROS_DOMAIN_ID=11` 已在 `~/.bashrc` 中配置，新终端自动生效
> - 工作空间使用 `--symlink-install`：**Python 文件改完直接生效，C++ 文件需重新 build**

### 1.2 启动前检查清单

| 检查项 | 命令 | 预期 |
|--------|------|------|
| WiFi 主 IP | `ip addr show wlP1p1s0` | `192.168.31.91/24` |
| USB 网卡(雷达直连) | `ip addr show enx00e04c680779` | `192.168.1.100/32` |
| 有线网口 | `ip addr show enP8p1s0` | `10.0.0.48/24` |
| 雷达可达 | `ping -c 2 192.168.1.195` | 通 |
| UpBoard 可达 | `ping -c 2 10.0.0.6` | 通 |
| D435i 已接 | `ls /dev/video*` | 有设备 |
| dog-brain 状态 | `systemctl status dog-brain` | 按需 |

---

## 2. 网络配置与维护

### 2.1 当前网络拓扑

```
                    ┌─────────────────────────────┐
                    │   Jetson Orin NX (大脑)       │
                    │                               │
  WiFi 路由器 ──────┤  wlP1p1s0: 192.168.31.91/24  │
  (192.168.31.x)    │                               │
                    │  enx00e04c680779 ─────────────┼──── Mid-360 雷达
                    │  (USB 网卡) 192.168.1.100/32  │     (192.168.1.195)
                    │                               │
                    │  enP8p1s0: 10.0.0.48/24  ─────┼──── UpBoard 小脑
                    │                               │     (10.0.0.6)
                    └─────────────────────────────┘
```

> ⚠️ **2026-08-25 变更：** 雷达改走 USB 网卡直连，WiFi 不再配 192.168.1.100 辅助 IP（之前双 IP 冲突导致点云链路建不起来）。

### 2.2 WiFi 静态 IP 配置

已用 nmcli 持久化，重启自动生效（**仅主 IP，无辅助 IP**）：

```bash
# 查看当前配置
nmcli -t -f ipv4.addresses,ipv4.method connection show Xiaomi_A389

# 若需重新设置（一般不需要）
sudo nmcli connection modify Xiaomi_A389 \
  ipv4.method manual \
  ipv4.addresses "192.168.31.91/24" \
  ipv4.gateway 192.168.31.1 \
  ipv4.dns "192.168.31.1,8.8.8.8"
sudo nmcli connection up Xiaomi_A389
```

### 2.3 USB 网卡配置（雷达直连）

雷达通过 USB 网卡 `enx00e04c680779` 直连，IP 192.168.1.100/32：

```bash
# 查看状态
ip addr show enx00e04c680779

# 若需重新配置（一般不需要，nmcli 已持久化）
sudo nmcli connection modify enx00e04c680779 \
  ipv4.method manual \
  ipv4.addresses "192.168.1.100/32"
sudo nmcli connection up enx00e04c680779
```

> ⚠️ **教训：** 若 WiFi 同时配 192.168.1.100/24 辅助 IP，会与 USB 网卡冲突，驱动握手成功但无点云。修复：移除 WiFi 辅助 IP。

### 2.4 LCM 组播绑定

LCM 组播绑定在 `enP8p1s0`（有线网口），已在 `bridge_params.yaml` 中写死 `ifname=enP8p1s0`，无需额外配置。

---

## 3. 建图流程

### 3.1 启动建图

**方式一：systemd（推荐）**
```bash
sudo systemctl start dog-brain
# 等待 12s (ExecStartPre sleep) + 约 10s 雷达初始化
```

**方式二：手动启动**
```bash
bash /home/nvidia/restart_stack.sh
# 或
source /opt/ros/humble/setup.bash
source /home/nvidia/dog_ws/install/setup.bash
ros2 launch dog_brain bringup_launch.py mode:=mapping
```

**省电模式（关闭 D435i 相机）：**
```bash
# 建图只需雷达+IMU，可关闭相机降功耗（防断电）
sudo systemctl start dog-brain  # 服务已配置 enable_camera:=false
# 或手动启动时传参
ros2 launch dog_brain bringup_launch.py mode:=mapping enable_camera:=false
```

### 3.2 建图操作步骤

```
1. 启动建图栈 → 静止 5~10s 让 IMU 初始化
2. 遥控/推动狗走一圈（建图区域）
3. RViz 中观察 /cloud_registered 点云是否正常（禁订 /Laser_map！）
4. 存图: ros2 service call /map_save std_srvs/srv/Trigger
5. 重命名保留: mv test.pcd map_$(date +%Y%m%d_%H%M).pcd
6. 如需继续建图，重复 2-5（每次存图覆盖 test.pcd）
```

### 3.3 存图后部署到导航

```bash
# 将建图产出的 PCD 复制到导航地图位置
cp map_*.pcd /home/nvidia/dog_ws/src/dog_brain/maps/lab_3d_map.pcd

# symlink-install 不链接 build 后新增文件，需手动同步到 install
ln -sf /home/nvidia/dog_ws/src/dog_brain/maps/lab_3d_map.pcd \
       /home/nvidia/dog_ws/install/dog_brain/share/dog_brain/maps/lab_3d_map.pcd
```

> **←订正2026-09-20：这一步是"换图"的唯一生效路径，不能省略、也不能只把新 PCD 放到别处。**
> 因为生产启动脚本 `nav_restart.sh` 调 `bringup_launch.py mode:=navigation` 时**未传 `map_pcd` 参数**，
> 导航始终取 launch 默认值，即上表这个文件本身。当前该文件为 2026-09-18 新图
> （51973789 字节≈52MB，1624173 点）。另：`pcd_to_map_node.py` 的 2D 图也从同一份 PCD
> 实时投影，所以替换后 2D 图坐标系会一并更新，无需另行处理。

### 3.4 建图注意事项

- **远程 RViz 禁订 `/Laser_map`**（1.17GB 洪流会饿死 /tf 并触发雷达失同步）
- 远程只看 `/cloud_registered`（~127KB/帧，安全）
- 建图途中务必先 `/map_save` 存增量，再停栈（否则进程内地图丢失）

---

## 4. 导航流程

### 4.1 前置条件

- [x] 已有 PCD 地图 (`src/dog_brain/maps/lab_3d_map.pcd`，2026-09-18 新图：51973789 字节≈52MB、1624173 点) ←订正2026-09-20
- [x] install 路径 symlink 已建立
- [x] WiFi 双 IP 正常

### 4.2 启动导航

**推荐方式：使用清理脚本**
```bash
bash /home/nvidia/nav_restart.sh
# 自动清理所有残留节点 → 启动导航模式
# 日志输出到 /home/nvidia/bringup_nav.log
```

**手动方式**
```bash
source /opt/ros/humble/setup.bash
source /home/nvidia/dog_ws/install/setup.bash
ros2 launch dog_brain bringup_launch.py mode:=navigation
```

### 4.3 导航操作步骤

```
1. 启动导航栈 → 等 60s 让 FAST-LIO 初始化
2. 确认 /Odometry 有数据 (~10Hz):
   ros2 topic hz /Odometry
3. 标定初始位姿（二选一）:
   a. RViz 中点击 "2D Pose Estimate" 在地图上标定狗的位置
   b. 命令行: ros2 run fast_lio_localization publish_initial_pose.py <x> <y> 0.45 <yaw> 0 0
4. 确认 ICP 定位收敛:
   ros2 topic echo /map_to_odom --once   # fitness > 0.9 即收敛
5. 遥控器切换到导航档: SWE(急停 off) → SWG(站立 RL) → SWF(导航档 1800)
6. RViz 中点击 "2D Goal Pose" 发导航目标
```

### 4.4 导航注意事项

- **不要用 sudo 启动**（root 身份下 Python 找不到 tf_transformations）
- **注意 conda 环境干扰**：`~/.bashrc` 中 `conda init` 会把 `python3` 指向 miniconda（缺 numpy/tf_transformations），导致定位四件套（global_map_publisher / pcd_to_map / elevation_map / step_detector / global_localization）全部崩溃，症状是 `/localization` 无数据、map→base_link TF 缺失。`nav_restart.sh` 已内置 PATH 剔除 conda 的修复；手动启动导航前需先 `conda deactivate`
- **体素层 z 窗口（R4，R7 验证安全）**：当前配置 -0.75~1.65m，垂直分辨率 0.15，z_voxels=16，实测闭环误差 0.17m。2026-08-28 R7 回退 B1 后 5分钟运动 z 稳定 0.24~0.48m，oob=0，窗口安全。
- 每次重拉栈后必须重新给 initialpose
- 遥控器 SWF 通道(ch10) = 导航档，1800μs

---

## 5. systemd 服务管理

### 5.1 dog-brain.service（建图模式）

```bash
# 查看状态
sudo systemctl status dog-brain

# 启动/停止/重启
sudo systemctl start dog-brain
sudo systemctl stop dog-brain
sudo systemctl restart dog-brain      # 失同步时一键恢复

# 开机自启管理
sudo systemctl enable dog-brain       # 启用开机自启
sudo systemctl disable dog-brain      # 取消开机自启

# 查看日志
journalctl -u dog-brain -f            # 实时跟踪
journalctl -u dog-brain -n 60         # 最近 60 行
```

**服务配置**（`/etc/systemd/system/dog-brain.service`）：
- 用户: nvidia，环境变量: `ROS_DOMAIN_ID=11`（2026-08-25 补上，之前缺失导致建图栈跑在默认 0 域）
- 模式: mapping（建图）
- ExecStartPre: sleep 12（等网络/雷达就绪）
- Restart: on-failure（崩溃自动重启，间隔 8s）
- TimeoutStopSec: 20

### 5.2 导航模式 systemd（✅ 2026-09-24 已建）

**`dog-nav.service`（user 级）已部署并 enable，开机自动进入导航**，无需再手动
`nav_restart.sh`。详见本文开头 §「★ 2026-09-24 开机自启（导航+协同）+ 初始位姿时序修复」。

```bash
systemctl --user status  dog-nav        # 状态
systemctl --user restart dog-nav        # 重启导航栈
journalctl --user -u dog-nav -f         # 日志
```

**手动启动方式仍保留**（需要时可用，如调试）：
```bash
bash /home/nvidia/nav_restart.sh        # 注意：会与 dog-nav 冲突，先 systemctl --user stop dog-nav
```

**建图模式**（`dog-brain.service`，system 级，已 disabled 不自启）需手动启动：
```bash
systemctl --user stop dog-nav && sudo systemctl start dog-brain
```

---

## 6. 日常运维脚本

| 脚本 | 路径 | 功能 | 使用场景 |
|------|------|------|---------|
| `restart_stack.sh` | `/home/nvidia/` | 清理残留 + 重拉建图栈 | 建图模式重启 |
| `stop_stack.sh` | `/home/nvidia/` | 清理所有 ROS 节点 | 完全停止 |
| `nav_restart.sh` | `/home/nvidia/` | 彻底清理 + 重拉导航栈 | 导航模式重启 |
| `watchdog_run.sh` | `/home/nvidia/` | 看门狗测试脚本 | 测试用 |

### 6.1 脚本工作原理

所有重启脚本遵循相同模式：
```
1. pkill -f 各节点进程名（精确匹配，避免误杀 SSH）
2. sleep 2（等待进程退出）
3. pgrep -af 检查残留
4. source ROS 环境
5. nohup setsid ros2 launch ... &（后台启动，脱离 SSH 会话）
```

### 6.2 ⚠️ 为什么不能在 SSH 命令行直接 pkill

```bash
# ❌ 危险！会匹配并误杀自己的 SSH 会话
pkill -f bringup_launch.py

# ✅ 正确：使用脚本文件（脚本内 pkill 不影响调用者）
bash /home/nvidia/restart_stack.sh
```

---

## 7. 故障诊断与处置

### 7.1 故障速查表

| 故障 | 症状 | 处置 |
|------|------|------|
| **雷达冷启动卡死** | LED 亮、ping 通、驱动 init success，但点数=0 | `sudo systemctl restart dog-brain` + 静止 60s |
| **雷达数据链路冻结** | 握手成功（mode Normal）但无点云，多次崩溃后出现 | **雷达断电 10s 再上电**，然后重启栈 |
| **雷达掉线→导航栈崩溃** | FAST-LIO 位姿发散到数百米+3D 倾斜，导航栈随之崩溃（2026-08-31 实验③） | 重启雷达+restart 栈；雷达稳定前不发导航目标；待查根因（网线/接头/驱动） |
| **失同步** | FAST-LIO 狂刷 `not Synced` + `No Effective`，位姿发散 | 先 `/map_save`（建图中），再 restart |
| **ICP 定位丢失** | fitness=0.000，`Robot is out of bounds` | 重新给 initialpose |
| **USB 网卡 IP 丢失** | mid360 报 `bind failed` | 重配 USB 网卡 IP（见 §2.3） |
| **WiFi/USB 网卡 IP 冲突** | 驱动握手成功但无点云 | 移除 WiFi 辅助 IP，仅保留 USB 网卡 192.168.1.100 |
| **体素层避障失明（z 漂移）** | costmap 报 `Sensor origin ... is out of map bounds` | ✅ 已解决（R7，2026-08-28）：根因是 B1 修改(odom.header.stamp)引入 1.3~1.5s 延迟→ICP pitch 异常→z 越界。回退 B1 后 5分钟运动 oob=0。若复现先检查 transform_fusion.py 第75/94行是否为 get_clock().now() |
| **TF extrapolation into the past** | 代价地图报 `Lookup would require extrapolation into the past`，点云被丢 | 启动期偶发（等 ICP 初始化 1~1.4s），initialpose 自动发布后消失。B1 修改曾用 odom.header.stamp 解决但引入 1.5s 延迟，已回退（R7）。当前 now() 版本启动后无此问题 |
| **planner_server extrapolation** | planner_server 报 `Lookup would require extrapolation into the past`，Requested time 固定不变，earliest data 不断前进 | **诱因**：导航 Goal 未完成时直接遥控接管，切回后继续旧 Goal，Goal 时间戳被 TF buffer 淘汰。不影响 costmap/避障。**预防**：遥控前先 Cancel Goal，回来后发新 Goal。待对照实验确认（见技术文档 R7） |
| **点云超期警告** | local_costmap 报 `observation buffer has not been updated for 0.3X seconds`，持续 0.57 条/秒 | expected_update_rate=0.3(3.33Hz) 设置偏高，点云实际发布频率不稳定。不影响功能，可调整 expected_update_rate 或优化点云发布频率 |
| **publish_initial_pose 崩溃** | 导航启动后 auto_initial_pose 节点 exit code 1，ICP 未初始化 | 检查日志中崩溃原因，确认 conda 环境是否已剔除（nav_restart.sh 已内置），手动执行 `ros2 run fast_lio_localization publish_initial_pose.py --auto` 测试 |
| **DDS 抽风** | ros2 topic echo/hz 空返回 | 重试 2-3 次 |
| **相机断流** | local_costmap 报 camera buffer 延迟 | 检查 transform_tolerance 是否 2.0 |
| **/global_map 读到 0 点** | ICP 启动但 fitness=0 | 检查 install 路径 symlink |
| **Nav2 服务器不 active** | 启动后部分服务器 crash | 检查插件类名、BT 库、观测源 |
| **锁存话题过期** | /map /costmap 有数据但已停止发布 | 先验新鲜度（header.stamp vs 当前时间） |
| **QoS 不匹配** | 订阅不到 /map 等锁存话题 | 订阅端用 TRANSIENT_LOCAL + RELIABLE |

### 7.2 失同步详细处置

```bash
# 1. 诊断
journalctl -u dog-brain -n 60 | grep -iE "not Synced|No Effective"

# 2. 如果正在建图，先存图
ros2 service call /map_save std_srvs/srv/Trigger

# 3. 重启
sudo systemctl restart dog-brain
# 或
bash /home/nvidia/restart_stack.sh
```

### 7.3 ICP 定位收敛检查

```bash
# 查看定位结果（fitness > 0.9 为收敛）
ros2 topic echo /map_to_odom nav_msgs/msg/Odometry --once

# 查看 ICP 节点日志
ros2 topic hz /map_to_odom   # 应为 ~1Hz
```

### 7.4 雷达数据检查

```bash
# 雷达点云频率（应 ~10Hz）
ros2 topic hz /livox/lidar

# IMU 频率（应 ~200Hz）
ros2 topic hz /livox/imu

# 点云消息数
ros2 topic echo /livox/lidar --once | grep -c "x:"
```

---

## 8. 代码修改与构建

### 8.1 构建命令

```bash
cd /home/nvidia/dog_ws
colcon build --symlink-install

# 只构建特定包
colcon build --symlink-install --packages-select dog_brain
colcon build --symlink-install --packages-select fast_lio_localization
```

### 8.2 修改生效规则

| 文件类型 | 修改后是否需 rebuild | 说明 |
|---------|-------------------|------|
| Python 脚本 (.py) | ❌ 不需要 | symlink-install 直接链接源文件 |
| Launch 文件 (.py) | ❌ 不需要 | 同上 |
| Config 文件 (.yaml/.json) | ❌ 不需要 | 同上 |
| URDF 文件 (.urdf) | ❌ 不需要 | 同上 |
| C++ 源文件 (.cpp/.h) | ✅ 需要 | `colcon build --symlink-install` |
| CMakeLists.txt | ✅ 需要 | 同上 |

### 8.3 C++ 修改后

```bash
cd /home/nvidia/dog_ws
colcon build --symlink-install --packages-select livox_ros_driver2
# 或
colcon build --symlink-install --packages-select fast_lio_localization
source install/setup.bash
```

### 8.4 Git 操作

```bash
cd /home/nvidia/dog_ws
git status
git diff <file>
git add <file>
git commit -m "描述"
git push origin main    # ⚠️ 需用户明确批准
```

> **铁律**：不要擅自 git push，除非用户明确说"推送/提交到远程"。

---

## 9. 远程可视化 (RViz)

### 9.1 VM 连接 Jetson

- Parallels 必须 **「桥接」网络**（不要用共享/NAT）
- VM 需拿到 `192.168.31.x` 网段
- 确认 `ping 192.168.31.91` 通
- `ROS_DOMAIN_ID=11`（与 Jetson 一致）
- VM 上需先 `colcon build --symlink-install` 过 `dog_description`

### 9.2 RViz 推荐配置

**建图模式：**
```
Fixed Frame: odom
- TF
- RobotModel (/robot_description)
- PointCloud2: /cloud_registered (实时, ~127KB/帧)
- Path: /path
```

**导航模式：**
```
Fixed Frame: map
- TF
- RobotModel
- PointCloud2: /cloud_registered
- Map: /map (2D OccupancyGrid)
- Map: /global_costmap/costmap_raw
- Map: /local_costmap/costmap_raw
- Path: /plan, /local_plan
```

### 9.3 ⚠️ 远程 RViz 禁令

**绝对不要远程订阅 `/Laser_map`！**

- 单条消息 73M 点 ≈ 1.17GB
- 会打满网络链路 → /tf 被饿死 → 所有帧消失
- 可能压垮 USB 雷达链路 → 失同步 → 建图发散

替代方案：
1. Jetson 本地 RViz 看 /Laser_map
2. 存 PCD 拷回 VM 离线看
3. 远程只看 /cloud_registered（安全）

---

## 10. UpBoard 端操作

### 10.1 SSH 连接（经 Jetson 跳板）

```bash
sshpass -p xjzx ssh -J nvidia@192.168.31.91 xjzx@10.0.0.6
# 或从 Jetson 上:
ssh xjzx@10.0.0.6   # 密码: xjzx
```

### 10.2 UpBoard 控制程序

```bash
cd /home/xjzx/robot-software
./run_mc.sh ./mit_ctrl m r f
# m=mode(RL), r=run, f=fast
```

### 10.3 遥控器通道说明

| 通道 | 开关 | 功能 |
|------|------|------|
| ch4 (SWE) | 急停 | 始终有效，任何时候可急停 |
| ch5 (SWG) | 站立 RL | 切换到 RL 策略控制 |
| ch10 (SWF) | 导航档 | 1800μs = 导航模式，TCP 指令生效 |

---

## 11. 安全红线

### 11.1 绝对不可违反

1. **动狗/UpBoard 前**：备份 + 狗趴下 + 人盯 + 遥控器在手（SWE 急停始终有效）
2. **不擅自 git push**（除非用户明确批准）
3. **UpBoard 官方代码尽量不动**

### 11.2 操作规范

| 规则 | 说明 |
|------|------|
| pkill 必须写脚本 | 不能在 SSH 命令行直接 pkill -f（会杀自己 SSH） |
| 不要用 sudo 启动导航 | root 身份下 Python 找不到 tf_transformations 包 |
| 启动后等 60s | 让雷达/FAST-LIO 初始化完成 |
| 每次重拉栈后给 initialpose | ICP 定位需手动触发 |
| 服务调用三步走 | list → type → call，禁止凭记忆猜 |
| 单次 topic echo 失败重试 | 重试 2-3 次再下结论（DDS 间歇抽风） |
| 改配置前先备份 | 一次只改一个逻辑变量 |
| 新脚本必须 chmod +x | colcon install(PROGRAMS) 要求可执行 |

---

## 12. 多机协同与地图共享

### 12.1 系统架构

> ←订正09-11：本表为 09-08 版。**09-09 域归属定稿后 robot1 改为域 21**（见文首《多机协同接入》节）；
> 下表 robot1 的 Domain 11 与通信架构行的 `robot1/robot2 (Domain 11)` 均为旧值。

| 车辆 | IP | Domain | 定位方式 | 地图格式 | 地图路径 |
|------|-----|--------|---------|----------|----------|
| 主车 | 192.168.31.43 (nvidia) | 1 | RTAB-Map ICP (`Reg/Strategy=1`) | `my_room.db` + `my_map.yaml/pgm` | `~/wheeltec_ros2/src/wheeltec_robot_rtab/` |
| robot1 小车 | 192.168.31.47 (sunrise) | **21**（旧文写 11） | AMCL | 2D 栅格 | `~/robot1_ws/src/robot1_nav/maps/` |
| 机械狗 (robot2) | 192.168.31.91 (nvidia) | 11 | FAST-LIO2 + ICP | 3D PCD + 2D 栅格 | 3D 地图：`~/dog_ws/src/dog_brain/maps/lab_3d_map.pcd`；2D 导出：`~/dog_ws/maps/`　←订正2026-09-20（旧文只写 `~/dog_ws/maps/`，那是 2D 导出目录，非导航加载的 3D 图） |

**通信架构：**
```
主车 (Domain 1) ← domain_bridge → robot1 (Domain 21) / robot2 (Domain 11)
```

桥接的 6 个话题 (每车):
- `/robot_N/soldier_pose` (11→1): 位姿上报
- `/robot_N/odom` (11→1): 里程计上报  
- `/robot_N/battery_state` (11→1): 电池状态
- `/robot_N/cmd_vel` (1→11): 速度控制下行
- `/robot_N/goal_pose` (1→11): 导航目标下行
- `/robot_N/initialpose` (1→11): 初始位姿下行

### 12.2 地图共享方案

**策略：机械狗建图 → 多格式导出 → 三车共用**

```
机械狗 Mid-360 建图 (精度最高)
  │
  ├─ 3D PCD → 机械狗自己用 (ICP 定位)
  │
  └─ 2D OccupancyGrid (export_2d_map.py 导出)
       ├─ 主车用: my_map.yaml/pgm (Nav2 导航)
       ├─ robot1 用: 2D 地图 (AMCL 定位)
       └─ 主车 .db: 与主车在用 2D 图同坐标系（RTAB-Map 定位）　←订正2026-09-20：实际做法是变换数据库节点位姿使之与狗图坐标系一致，而非"同一原点重建"
```

**关键：所有车从同一物理原点出发建图，坐标系自动对齐。**

> **←订正2026-09-20：该策略已于 2026-09-18 实做并验证。** 狗在实验室重新建图（走行约 120m 闭合
> 回环、终点闭合误差 0.0m、Z 漂移 0.003m），2D 图 origin 落为 (−12.812, −15.218)；主车在同一点位
> 建图后与狗图配准融合（墙体匹配中位偏差 5cm、容忍 20cm 时重合 93.1%），并用坐标变换把主车建图
> 数据库的 170 个节点位姿搬到同一坐标系。两车实测 2D 图 origin 一致（差 <0.001m），位姿同一坐标系，
> 可互发同一地图坐标。**维护纪律：任何一方重新建图后，另一方若要继续共用，须重新对齐坐标系。**

### 12.3 地图导出工具

```bash
# 基本用法: 从默认路径读取，输出到 ~/dog_ws/maps/
python3 ~/dog_ws/src/fast_lio_localization/scripts/export_2d_map.py

# 指定输入输出，命名为主车的 my_map
python3 ~/dog_ws/src/fast_lio_localization/scripts/export_2d_map.py \
  --pcd ~/dog_ws/src/dog_brain/maps/lab_3d_map.pcd \
  --output ~/dog_ws/maps --name my_map

# 直接推送到主车地图目录 (自动 SCP)
python3 ~/dog_ws/src/fast_lio_localization/scripts/export_2d_map.py \
  --pcd map.pcd --push-to-main

# 推送到 robot1 地图目录 (命名需为 lab_map)
python3 ~/dog_ws/src/fast_lio_localization/scripts/export_2d_map.py \
  --pcd map.pcd --name lab_map --push-to-robot1

# 推送到所有车辆 (主车 + robot1)
python3 ~/dog_ws/src/fast_lio_localization/scripts/export_2d_map.py \
  --pcd map.pcd --push-to-all
```

**三车地图路径：**
```
主车:   ~/wheeltec_ros2/src/wheeltec_robot_rtab/my_map.yaml + my_map.pgm
robot1: ~/robot1_ws/src/robot1_nav/maps/lab_map.yaml + lab_map.pgm
机械狗: ~/dog_ws/maps/ (本地导出)
```

### 12.4 多机协同服务管理

**机械狗端 (192.168.31.91)：**
```bash
systemctl --user status robot2-soldier.service
systemctl --user restart robot2-soldier.service
```

**主车端 (192.168.31.43)：**
```bash
systemctl --user status robot2-domain-bridge.service
systemctl --user restart robot2-domain-bridge.service
```

### 12.5 主车包使用状态

**正在使用：**
- `largemodel` — 大模型服务 (model_service, action_service)
- `mqtt_bridge_ros2` — MQTT 桥 (system_manager + webrtc + web_console)
- `turn_on_wheeltec_robot` — 底盘驱动 + 传感器
- `wheeltec_lidar_ros2` — lslidar 激光雷达
- `wheeltec_robot_rtab` — RTAB-Map + Nav2 导航
- `navigation2-humble` — Nav2 核心
- `domain_bridge` — 多车桥接

**未使用/备用：**
- `wheeltec_robot_nav2` — 旧版导航 (WHEELTEC.yaml)
- `wheeltec_multi` — 旧版多车方案
- `wheeltec_robot_slam` — 旧版 SLAM
- 其他: aruco, auto_recharge, ollama, tts, usb_cam 等

---

## 13. 速查表

### 13.1 IP / 端口 / Topic

| 项 | 值 |
|----|----|
| Jetson SSH | `nvidia@192.168.31.91` |
| Jetson WiFi IP | `192.168.31.91/24` |
| Jetson USB 网卡 | `enx00e04c680779` → `192.168.1.100/32`（雷达直连） |
| UpBoard | `10.0.0.6` (TCP :3333) |
| Mid-360 雷达 | `192.168.1.195` |
| LCM 组播 | `udpm://239.255.76.67:7667` |
| ROS_DOMAIN_ID | `11` |
| `/livox/lidar` | 雷达点云 (CustomMsg, 10Hz) |
| `/livox/imu` | 雷达 IMU (Imu, 200Hz) |
| `/camera/depth/color/points` | 相机点云 (PointCloud2, ~28.9Hz) |
| `/Odometry` | FAST-LIO 里程计 (10Hz) |
| `/odom` | UpBoard 里程计 (500Hz, 不进 SLAM) |
| `/cloud_registered` | 去畸变点云 (10Hz, ~127KB/帧) |
| `/map` | 2D 栅格地图 (OccupancyGrid) |
| `/cmd_vel` | 速度指令 (Twist) |
| `/map_to_odom` | ICP 定位结果 (1Hz) |
| `/joint_states` | 16 关节状态 (500Hz) |

### 13.2 常用命令

```bash
# 环境
source /opt/ros/humble/setup.bash && source /home/nvidia/dog_ws/install/setup.bash

# 建图
bash /home/nvidia/restart_stack.sh           # 重拉建图栈
bash /home/nvidia/stop_stack.sh              # 停止所有节点
sudo systemctl restart dog-brain             # systemd 重启建图

# 导航
bash /home/nvidia/nav_restart.sh             # 重拉导航栈

# 存图
ros2 service call /map_save std_srvs/srv/Trigger

# 定位
ros2 run fast_lio_localization publish_initial_pose.py 0 0 0.45 0 0 0

# 调试
ros2 topic list                              # 所有 topic
ros2 topic hz /Odometry                      # 频率
ros2 topic echo /map_to_odom --once          # 单条消息
ros2 node list                               # 所有节点
ros2 run tf2_tools view_frames               # TF 树

# 网络
ip -br addr show                             # 所有接口 IP
ping -c 2 192.168.1.195                      # 雷达
ping -c 2 10.0.0.6                           # UpBoard
nmcli connection show                        # 连接状态
```

### 13.3 文件位置

| 文件 | 路径 |
|------|------|
| 工作空间 | `/home/nvidia/dog_ws` |
| 导航地图 | `src/dog_brain/maps/lab_3d_map.pcd`（2026-09-18 新图：51973789 字节≈52MB、1624173 点）←订正2026-09-20 |
| FAST-LIO 存图 | `test.pcd`（工作目录） |
| Nav2 配置 | `src/dog_brain/config/nav2_3d_params.yaml` |
| 雷达配置 | `src/dog_sensors/config/mid360_config.json` |
| FAST-LIO 配置 | `src/fast_lio_localization/config/mid360.yaml` |
| URDF | `src/dog_description/urdf/dog.urdf` |
| systemd 服务 | `/etc/systemd/system/dog-brain.service` |
| 建图日志 | `/home/nvidia/bringup_test.log` |
| 导航日志 | `/home/nvidia/bringup_nav.log` |
| bashrc | `~/.bashrc`（含 ROS_DOMAIN_ID=11） |

---

## 文档同步红线（原 SYNC_BASELINE，补充 §11 之外的同步约束）

- 本 3 份文档为权威工作副本；iCloud / 其它镜像**仅在用户明确下令时**才同步（不自动复制回镜像）。
- 维护活文档默认**只增不改**；新内容追加为新章；覆盖旧结论需显式声明「取代上文」。
- 每次动狗 / UpBoard 前后，把健康检查快照刷新到最新状态。
- 硬件安全与「不擅自 git push」见上文 §11，此处不重复。

## 任务清单（合并自 TASK_LIST / PROJECT_STATUS，2026-08-27 校正）

### ✅ 已完成要点
硬件集成（三网口+LCM+TCP）、传感器驱动（Mid-360/D435i/时间戳/外参）、SLAM 建图、ICP 3D 定位（替换 AMCL）、Nav2 3D 导航（全链路+端到端+R1~R4 调优）、控制链（TCP 小端+注入部署+实测驱动）、多机协同（三车地图共享）、文档体系、B1时间戳对齐与回退（R5/R7，2026-08-27/28）、逐层延迟定位法（A→B1→B2单变量实验）。

### ✅ P0 阻塞项（已清零）
1. ~~z 漂移根因修复~~ ✅ 已解决（R7，2026-08-28）：回退 B1 时间戳修改后，延迟 21~43ms，z/pitch 稳定，5分钟运动无 oob。

### 🟡 P1 重要待办
1. 导航启动问题排查：publish_initial_pose.py 偶发崩溃(exit code 1)、/livox/lidar 偶发无数据
   ← 订正2026-09-24：**`publish_initial_pose` 时序问题已根治**（`4940852`，`--map-grace` 8s→70s，覆盖 60s 地图发布周期）；`/livox/lidar` 偶发无数据仍待观察
2. ~~导航常驻 systemd（建 dog-nav.service，开机自启）~~ ✅ **已完成（2026-09-24）**：`dog-nav.service`（user 级）+ `robot2-adapter` 均 enable，`linger=yes`；`dog-brain`（建图）已 disable。详见文首 §「★ 2026-09-24 开机自启」
3. ~~全向横移扩展（Nav2 全向 + TCP 加第 4 个 double=vy + UpBoard 注入 v_des[1]）~~ ✅ **已完成（2026-09-21）**：三层全打通，实测 `linear.y=0.40 → 狗左移 0.37m`（符号正确）。见 §「★ 2026-09-21 …四、横移（vy）已贯通」
4. 建图模式 systemd 启用（当前 disabled，需 enable dog-brain）

### 🟢 P2 优化项
1. ~~B1 第二阶段~~ ❌ 已取消（会引入延迟）
2. planner_server goal 时间戳过旧：增大 TF cache_time 或处理 goal 时间戳（0.08条/s，不影响 costmap）
2. 失同步自动看门狗（连续 N 秒 No Effective Points → 自动 restart）
3. UpBoard 日志清理（689MB）
4. 建图漂移优化
5. RViz 远程降采样（/Laser_map_downsampled）

### 🔵 P3 远期（楼梯间专项，约 10-14 周）
楼梯检测 → RL 走楼梯验证 → 训练爬楼/越障模型 → 楼梯间导航参数 + ICP 定位验证 + 可通行处理 + 安全保护 → 集成验收。

## 已知限制（2026-08-27 校正）
1. 玻璃/金属走廊 ICP 定位失效（905nm 激光打玻璃无回波，物理限制）
2. 失同步不自愈（雷达掉线重连后时钟跳变，需手动 restart）
3. FAST-LIO /Odometry 快速转向时间隔 240ms（计算瓶颈，RViz 中位置滞后）
4. RL 模型无地形观测（57 维观测不含高度/楼梯信息）
5. 全向横移能力未启用（Nav2 差分 vy=0 + TCP 无 vy 槽位）
6. dog-brain.service 当前 disabled（需手动启动建图栈）
7. ~~Nav2 体素层 z 窗口过窄~~ ✅ 已修复（R4，2026-08-27，闭环误差 0.17m，窗口 -0.75~1.65m）
8. 雷达数据链路偶发冻结（多次崩溃后握手成功但无点云，需断电 10s 再上电）
9. **~~z 漂移导致长时导航失效~~（2026-08-27 发现，2026-08-28 已解决 R7）**：根因是 B1 修改(odom.header.stamp)引入 1.3~1.5s 延迟→ICP pitch 异常→z 耦合越界。回退 B1 后解决（见技术架构文档附录 E R7）。
10. **点云超期警告**：局部代价地图 expected_update_rate=0.3(3.33Hz)，实际点云发布频率不稳定，持续产生 observation buffer 超期警告（0.57条/秒），不影响功能但增加日志噪音
11. **导航启动偶发问题**：publish_initial_pose.py 偶发崩溃(exit code 1)、/livox/lidar 偶发无数据（驱动握手成功但不点云）
12. **~~FAST-LIO /Odometry twist 字段全零~~**（2026-08-31 发现，2026-09-04 R20 已补全）：线速度由 world/odom 系旋到 base 系；角速度为 IMU gyro 减 EKF 零偏。已编译，待下一轮实车核验符号、噪声和 DWB 响应。
13. **小倒向 -0.24 欠速 5~18%**（2026-08-31 发现）：疑 UpBoard deadband 0.075 在倒向 ÷2 后贴边切削（v_des=-0.08 距死区仅 0.005），大倒向无此现象
14. **雷达掉线致导航栈崩溃**（2026-08-31）：FAST-LIO 发散→costmap 失效→栈崩溃，根因待查（网线/接头/驱动）


---

## R8 状态更新（2026-08-28）

### 已解决
1. **planner_server 旧 Goal timestamp extrapolation**：通过 RefreshPlanningPose 自定义 BT 节点解决，将 Goal 时间戳刷新为 Time(0)，测试 A/B 验证 extrapolation=0
2. **B1 时间戳修改导致的 1.3~1.6s 全链路延迟**：已回退（R7），transform_fusion 保持 now()
3. **Z/pitch 异常**：回退 B1 后解决，z/pitch 稳定
4. **costmap 跟不上机器人**：回退 B1 后解决，延迟 1~30ms
5. **全局代价地图只发一帧**：always_send_full_costmap=true 解决
6. **ICP 永不初始化**：A5 自动初始位姿发布解决

### 当前剩余问题（按优先级）

#### P1：偶发 planner 规划失败
- 现象：GridBased: failed to create plan with tolerance 0.50，连续 2-3 次后进入 recovery
- 触发：首次规划成功后的重规划阶段（RateController 1Hz）
- 征兆：与点云超期（/cloud_registered_body 1.33s未更新）、controller loop missed 10Hz 同时出现
- 影响：瞬时，recovery（clear costmap）后通常恢复，不影响导航功能
- 根因判断：偶发运行时数据一致性问题，不是 planner 算法或地图问题（目标点 cost 验证全部 FREE）
- 下一步：证据闭环监控，抓住失败瞬间对齐 /cloud_registered_body、Costmap、controller loop、CPU 时间轴

#### P2：FollowPath halt timeout（伴随现象）
- 现象：Failed to get result for follow_path in node halt!
- 根因：规划失败后 BT 触发 recovery，halt FollowPath 时取消 controller_server action，20ms 内未响应（系统负载高）
- 结论：不是独立问题，是规划失败的伴随错误；不致命，recovery 后导航继续
- 暂不修改 default_server_timeout（20ms->1000ms 可能让异常恢复变慢）

### 稳定基线（R8）
| 模块 | 状态 |
|---|---|
| FAST-LIO | 20~50ms |
| transform_fusion (now()) | 实时性正常 |
| Costmap 更新 | 正常时 1~30ms |
| Z/pitch | 稳定，无漂移 |
| out of map bounds | 0 |
| planner 旧 Goal timestamp extrapolation | 0 |
| 偶发 planner 规划失败 | 瞬时，recovery 后恢复 |
| FollowPath halt timeout | 规划失败伴随现象 |

### 暂不修改的参数
- ICP 频率（1Hz->0.5Hz 会降低 CPU 但全局定位修正变慢）
- voxel expected_update_rate（只是容错参数，不解决真实延迟）
- default_server_timeout（20ms->1000ms 可能让异常恢复变慢）
- transform_tolerance

### 代码提交
- R7 (c782b6d)：B1回退 + A5自动初始位姿 + 全局代价地图修复 + 文档更新
- R8 (f792e0d)：RefreshPlanningPose BT节点 + 解决planner旧Goal时间戳extrapolation
- 本地领先 origin/main 7 个提交，未 push

---

## R9 状态更新（2026-08-28，第二轮）

### 本轮新增工作

1. **RL 运动模型深度源码分析**（UpBoard 端完整读取）
   - 读取 FSM_State_RL.cpp、RLConfig.h、DesiredStateCommand.cpp/h、rt_rc_interface.cpp、main_helper.cpp
   - 确认 Observation 57维 / Action 16维定义
   - 确认速度命令链路：stateDes[6]=vx, [7]=vy, [11]=yaw_rate
   - 确认 RL 控制频率：策略推理 50Hz，底层控制 500Hz

2. **deadbandRegion 实际值确认**（决定性证据）
   - 头文件显示 0.01，但实际运行的 libbiomimetics.so 与 bak_075 MD5 完全相同
   - **确认实际运行的 deadbandRegion=0.075**
   - 教训：以实际运行的二进制为准，不能只看头文件

3. **非线性速度映射（NAV_V_MIN=0.24）深度分析**
   - 确认设置原因：克服 deadband(0.075)，否则 Nav2 低速指令(<0.225m/s)被完全过滤
   - 副作用：低速段被大幅放大（0.01→0.24），是 DWB 预测与实际轨迹偏差的来源之一
   - 全面评估修改影响面，**暂不修改**，需先做实车速度标定

4. **站立时零输入还会前进问题排查**
   - 第一步：/cmd_vel 无数据发布 → 排除 Nav2 残留和非线性映射原因
   - 确认 TCP 超时(300ms)后 v_des 清零逻辑存在
   - 确认 DesiredStateCommand 每次调用 data.zero()，低通滤波衰减很快(~0.02s)
   - 当前问题未复现（Nav2 未完整运行），待复现后继续排查 C/D 原因

### 当前剩余问题（更新优先级）

#### P1：偶发 planner 规划失败（不变）
- 现象：GridBased: failed to create plan，连续2-3次后进入 recovery
- 征兆：与点云超期、controller loop missed 同时出现
- 下一步：证据闭环监控，抓住失败瞬间对齐时间轴

#### P2：RL 运动模型与 DWB 不匹配（新增，提升优先级）
- 现象：导航中四腿动作不协调、运动轨迹反常、偶尔贴障碍
- 根因方向：非线性映射导致低速段放大 + RL 实际速度响应与 DWB 假设不一致
- 下一步：实车速度响应标定实验（固定 cmd_vel 测实际 vx/vy/wz）

#### P3：站立零速前进（新增）
- 现象：站立状态下零输入时机械狗仍缓慢前进
- 排查进展：A/B 原因已排除，C/D 待验证
- 下一步：复现问题后在 UpBoard 添加逐级调试打印

#### P4：FollowPath halt timeout（降为伴随现象）
- 根因：规划失败后 BT 触发 recovery，halt FollowPath 时 20ms 超时
- 结论：不是独立问题，随 planner 失败减少而减少

### 稳定基线（R9，与 R8 一致 + 新增分析项）

| 模块 | 状态 |
|------|------|
| FAST-LIO | 20~50ms ✅ |
| transform_fusion (now()) | 实时性正常 ✅ |
| Costmap 更新 | 正常时 1~30ms ✅ |
| Z/pitch | 稳定，无漂移 ✅ |
| out of map bounds | 0 ✅ |
| planner 旧 Goal timestamp extrapolation | 0（RefreshPlanningPose）✅ |
| 偶发 planner 规划失败 | 瞬时，recovery 后恢复 🟡 |
| RL 运动模型 | 源码分析完成，阶梯①②实车标定通过 ✅ |
| 站立零速前进 | 排查中，A/B 已排除 🔍 |
| bridge 指令链 | R9 只抬底+倒向÷1.5，稳态 ±3~6% ✅ |
| /Odometry twist | 全零，待评估 DWB 影响 ⚠️ |
| 雷达稳定性 | 08-31 掉线致栈崩溃，根因待查 🔍 |

### 暂不修改的参数（R10 更新）
- ~~非线性速度映射 NAV_V_MIN~~ → 已通过 R9 标定（只抬底 0.24），实车验证 ±3~6%
- ICP 频率、voxel expected_update_rate、default_server_timeout、transform_tolerance（同 R8）

### 经验教训（新增，必须形成记忆）
1. **全面分析原则**：任何修改前必须读完整个数据链路所有层，列出对所有下游层的影响，不能抓到一点就改
2. **二进制为准**：头文件与实际运行的二进制可能不一致，以二进制为准（MD5对比确认）
3. **逐层单变量实验**：A→B1→B2 逐层添加变量，比直接看 CPU/猜原因高效
4. **RL 黑盒敬畏**：RL 策略是黑盒，任何输入变化都可能导致不可预测的行为变化

### 代码提交状态
- R7 (c782b6d)：B1回退 + A5自动初始位姿 + 全局代价地图修复
- R8 (f792e0d)：RefreshPlanningPose BT节点 + planner旧Goal时间戳修复
- R9 (7f8ba56)：bridge只抬底+倒向÷1.5补偿+DWB参数对齐
- R9文档 (5775829)：RL运动模型源码分析+deadband MD5确认+5层指令链+站立前进排查
- R10文档（待提交）：R9修复实车验证(阶梯①②通过+③作废)+雷达掉线故障+重测流程
- 本地领先 origin/main 约 7 个提交，未 push

---

## R10 状态更新（2026-08-31）

### R9 修复包实车验证
- 阶梯①（手动单变量）+ 阶梯②（自动标定扫速）均已通过
- 稳态恒等 ±3~6%、倒向全活（-0.45 比例 0.99）、超速消除（0.45→0.465）
- 动态参数验证：运动延迟 0.3~0.75s、停车滑行 0.08~0.29m → xy_goal_tolerance=0.40 合理

### 阶梯③作废
- 原因：雷达掉线 → FAST-LIO 位姿发散到数百米+3D 倾斜 → 导航栈崩溃
- 处置：待雷达稳定后重测 2m 直线（过冲<0.15m 验收）

### 新发现
1. FAST-LIO /Odometry 与 UpBoard /odom twist 字段全零（DWB 影响待评估）
2. 小倒向 -0.24 欠速 5~18%（疑 UpBoard 死区切削）
3. 标定脚本三处修复（TCP 预检、NameError、shutdown 死锁）

### 环境变更
- robot2-soldier 已 systemctl --user stop（幽灵 /cmd_vel 清除）
- UDP rmem_max 已修复（/etc/sysctl.d/99-livox-udp.conf，8MB）

---

## 09-03 晚状态更新（R15 实车验证 + 交接）

### 当前系统状态
| 模块 | 状态 |
|------|------|
| FAST-LIO | 正常 ✅ |
| ICP 定位 | fitness 1.000 ✅ |
| Nav2 | active ✅ |
| bridge_node | R15 版本，lock_sec=5.0 ✅ |
| wz floor | ON (0.19) ✅ |
| wz 方向锁 | ON (lock_sec=5.0) ✅ |
| dwell 解锁 | ON (2.0s) ✅ |
| TCP 连接 | ESTABLISHED 10.0.0.6:3333 ✅ |
| soldier | masked（不自动启动）✅ |

### R15 实车验证结果
| 轮次 | 目标 | 结果 | 备注 |
|------|------|------|------|
| R15-1 | 1m 直线 | ✅ PASS (0.089m) | 3.5s，wz 仅 1 flip |
| R15-2 | 2m 斜线+90° | ❌ FAIL | yaw 只到 55.2°，y 误差 0.705m |

### R15-2 FAIL 分析
- 根因：DWB 低频方向交替（5-6s 周期）穿透 lock_sec=2.0s 过滤器
- 190 次 wz 翻转 @ 0.86 flips/s，锁过滤到 10 次（5-6s 间隔），仍导致 yaw 振荡 ±30°
- 修复：lock_sec 2.0→5.0s，已部署并确认

### 待执行
- R15-2 重测（lock_sec=5.0 版本），目标 (3.068, -1.541) yaw=90°
- **发车必须获得用户当轮明确"发车"文字确认**

### 挂起项
1. ~~5 commits push（R11~R15，用户未批准）~~ ✅ **已完成** ←订正2026-09-11：仓库已全部同步，无待推送提交
2. A2 wz 符号滞回（可能被 R15 覆盖）
3. B1 main_helper accept 修复（核实部署二进制）

### 新 agent 接手须知
- 权威文档是本文件 + `技术架构与调参.md` + `README.md`
- 安全红线见 agent 记忆 `project-dog-safety-rules`
- 发车必须用户明确说"发车"，预告不等于批准（R11-4 撞墙教训）
- bridge 代码改完需同步 src + install site-packages + 清 .pyc
- nav_restart.sh 拉栈后检查 bridge 日志确认 R15 参数生效
- lock_sec=5.0 若转向仍不足可继续提高（上限建议 8s）

---

## R21 状态更新（2026-09-04）

### 当前部署

- `controller_server.odom_topic=/nav_odom`；BT navigator 仍使用 `/Odometry`，velocity_smoother 仍为 OPEN_LOOP。
- bridge 以当前时间发布控制专用 `/nav_odom`：`vx=最终桥接目标`、`vy=0`、`wz=UpBoard IMU 低通值`；限制为 `|vx|<=0.45m/s`、`|wz|<=0.35rad/s`。
- UpBoard IMU 超时 0.2s 后自动回退桥接角速度并告警；不更改 FAST-LIO `/Odometry`、TF、ICP、代价地图和 R18 方向锁/抬底。
- 源码与安装态已同步，`lcm_bridge`、`dog_brain` 编译成功；bridge 烟雾测试通过并已停止。

### 已知独立风险

- R20 `/Odometry` 年龄中位 1.347s，明显高于 R19 的 0.032s；同时有 27 次控制循环超时和 294 次点云过期。R21 只隔离 DWB 速度反馈，不解决该系统积压。
- 若下次启动后 `/Odometry` 年龄持续大于 0.2s，或点云/控制周期告警持续增长，必须停止发车并先处理性能/时间同步。
- UpBoard `/odom` twist 当前全零，不能用作真实线速度；`/nav_odom.linear.x` 暂为明确的开环下发值。

### 回滚位置

- 修改前文件：`/home/nvidia/rosbag_runs/20260904_nav_r20_retry_113238/analysis/pre_r21/`
- R20/R21 分析与测试脚本：`/home/nvidia/rosbag_runs/20260904_nav_r20_retry_113238/analysis/`


---

## R22 状态更新（2026-09-07）：雷达时间戳漂移与 FAST-LIO 稳态积压


## 1. 背景

R21 满电复测准备期间测得 `/Odometry` 年龄中位 **2.28s**，远超 0.2s 门槛。按 tech.md「下一轮验收」条款不得发车，转为定位与修复。

## 2. 三层定位法（本次建立，建议固化为标准流程）

| 层 | 测什么 | 期望 |
|---|---|---|
| ① 驱动层 | `/livox/lidar`、`/livox/imu` 的 receipt−header | <0.1s |
| ② FAST-LIO 算法层 | 输出戳 − 输入戳 | ≈ **+0.1s**（一个扫描周期）；偏离说明算法在加/减偏移 |
| ③ 队列层 | 收到输出时刻 − 对应输入戳 | 即处理+缓冲总延迟，应 <0.1s |

### ⭐ 测量铁律（本次血的教训）

**测高频大消息（点云）必须用 `raw=True` 订阅，只读 CDR 前 12 字节取戳，绝不反序列化。**

本次曾用 Python 常规订阅测 `/livox/lidar`，因每帧数百 KB 的反序列化跟不上 10Hz 而越落越多，得到「1.09s 且持续漂移」的**假象**，一度误判为发布延迟，走了很长弯路。小消息（IMU / Odometry）不受此影响。

```python
def raw_cb(raw):
    b = bytes(raw.data if hasattr(raw, 'data') else raw)
    sec, nsec = struct.unpack_from('<iI', b, 4)   # 跳过 4 字节 CDR 封装头
    return sec + nsec * 1e-9
```

## 3. 根因 1：Livox SDK 点云时间戳基于自由运行的设备钟（✅ 已修复）

**证据**（驱动内一次性诊断打印）：

```
[HOSTSTAMP] pkg.base_time=1788515271.861988 host_now=1788515274.861391 diff=2.999404
```

- 雷达设备钟**落后主机约 3.0 秒**，且以 **6~15 s/小时**持续漂移
- 后果：FAST-LIO 的 lidar 与 IMU 时间基准不一致 → 反复打印 `IMU and LiDAR not Synced`、`lidar loop back, clear buffer` → `/Odometry` 老化到 2.28s
- 雷达断电 10s、Jetson + 雷达同时冷启动**均无法消除**（数值可复现），不是偶发状态

**修复**：`src/livox_ros_driver2/src/lddc.cpp`

- 在最后一个 `#include` 之后插入 `LddcHostTimeNs()`（基于 `std::chrono::system_clock`）
- 三处 `header.stamp` 的 **ROS2 分支**改为主机时间：
  - `InitPointcloud2Msg` / `InitCustomMsg` / `InitImuMsg`
- **保留** `livox_msg.timebase`、`pkg.base_time`、逐点 `offset_time` 仍在设备域 → **帧内去畸变逻辑完全不受影响**
- 补丁脚本：`ji/work/remote_scripts/patch_lddc_hoststamp.py`（含设备域字段未被误改的断言）

**效果**：`/livox/lidar` 1.09s → **10.0ms**；`/livox/imu` → **0.6ms**；`not Synced` / `loop back` 归零。

## 4. 根因 2：FAST-LIO 稳态队列积压约 1.45s（❌ 未修复）

**实测**：输出戳 − 输入戳 = **+0.0993s**（算法完全正确）；队列延迟 = **1.42~1.45s**（稳态、不漂移）。

**已排除**：
- CPU 争用 —— 停掉 ICP 节点后 1.4522s → 1.4449s，**无变化**；FAST-LIO 自身仅 27.8%
- 失同步、丢帧 —— 输出间隔精确 0.1000s（10.00Hz）
- 驱动层 —— `/livox/lidar` 仅 10.0ms

**机制**：`sync_packages()` 取 `lidar_buffer.front()`（**最旧帧**）且**从不丢弃过期帧**。启动期 IMU 初始化会堆入约 14 帧，之后逐帧处理速率恰等于输入 10Hz，**队列永不排空**。

**待实施修复**（未动代码，等批准）：在 `sync_packages()` 开头、且仅在 `!lidar_pushed` 时——

```cpp
while (lidar_buffer.size() > 2) { lidar_buffer.pop_front(); time_buffer.pop_front(); }
```

**判断**：这很可能就是 R20 记录中「1.347s 全链路延迟」的**真身**——此前与根因 1 的设备钟偏移叠加在一起，从未被单独看清。R7 曾把类似延迟归因于 B1 时间戳修改，本次证据表明至少还叠加了这个队列因素。

## 5. 本轮新增常驻脚本

放在 Jetson 的 `/home/nvidia/`，本地副本在 `ji/work/remote_scripts/`：

| 脚本 | 用途 |
|---|---|
| `verify_preflight.sh` | **发车前门禁**：节点数、`odom_topic`、ICP fitness、失同步、告警计数、`/Odometry` 与 `/nav_odom` 年龄、`vy` 是否恒 0 |
| `measure_odom_age.sh` | `/Odometry` 年龄 75s 长窗，给出毛刺占比与漂移率 |
| `check_layers.sh` | 三层体检：驱动输出(raw) vs FAST-LIO 输出 |
| `measure_fastlio_delta.sh` | 输出戳−输入戳 + 队列延迟，判断滞后发生在哪一层 |
| `stop_all_ros.sh` | 彻底清理所有 ROS 进程（pkill 写在脚本内，避免误杀 SSH） |
| `restart_lidar_driver.sh` | 单独重启雷达驱动（同样规避 pkill 自杀问题） |
| `patch_lddc_hoststamp.py` / `patch_lddc_diag.py` | 主机时间戳补丁与 HOSTSTAMP 诊断日志补丁 |

## 6. 遗留待办

1. **实施「丢弃过期帧」补丁**并重编译验证 → `/Odometry` 回到 <0.1s 才能发车
2. `lddc.cpp` 的主机时间戳补丁**尚未提交**（安全红线：提交与 push 均需用户明确批准）
3. 补丁内一行一次性 `HOSTSTAMP` 诊断 `printf` **保留与否待定**（建议保留，时间问题排查很有用）
4. 验证通过后重跑 **R21 满电复测**：核心看转向增益是否从低电时的 0.769 / 0.516 回到 R20 的 0.912 / 0.874
5. 建议把「三层定位法 + raw 测量铁律」写进故障速查表

## 7. 本轮数据产物（Jetson）

- `~/rosbag_runs/20260904_nav_r21_hoststamp/nav_hoststamp`（18:02:25–18:03:34，仅空转未发车，包完整）
- `~/rosbag_runs/20260904_nav_r21_fullbat_r2/nav_fullbat2`（16:20–16:25，未发车，包完整）
- 更早的 `20260904_nav_r21_drive` 因狗没电硬断电损坏，已用页级抢救恢复 78.5 万条消息
  （工具：`ji/work/salvage_rosbag2_sqlite.py` + `identify_orphans.py` + `analyze_r21_salvage.py`）


---

## R23 状态更新（2026-09-07）：R21 满电复测通过，导航链路达标

### 复测条件

雷达主机时间戳补丁（R22）生效后拉栈，`verify_preflight.sh` 全绿
（`/Odometry` 中位 23.5ms、26 节点、ICP 1.000、无失同步）。发车录制
`~/rosbag_runs/20260907_nav_r21_retest/nav_retest`（443MB，包完整），
控制窗口 349.5s、9 个随机目标、累计转向 164.8s。

### 验收结论：通过，当前为项目史最佳状态

| 验收条款 | R20 | R21 低电轮(09-04) | **R21 满电复测(09-07)** |
|---|---:|---:|---:|
| `No valid trajectories` | 34 | 0 | **0** |
| `Controller patience exceeded` | 4 | 0 | **0** |
| DWB 输入任一越界占比 | 62.03% | 0% | **0%**（窗口内 17464 帧，`vy` 恒 0） |
| `imu_stale` | — | 0 | **0** |
| `/Odometry` 年龄中位（带载+录制） | 1.347s | 0.030s | **25.4ms** |
| 目标结果 | 2成/2抢(4) | 7成/3抢(10) | **7成/2抢/0abort(9) = 78%** |
| `extrapolation` / `oob` / `spin_timeout` | — | 0 | **0 / 0 / 0** |

反事实证据保持：FAST-LIO `/Odometry` 窗口内仍有 31.04% 样本越界
（`vy|max|=0.755`），隔离 `/nav_odom` 的设计持续有效。

### 转向链路对比（R21 复测核心问题）

| 指标 | R20(满电) | R21 低电轮 | **R21 满电复测** |
|---|---:|---:|---:|
| 相关系数 | 0.912 | 0.769 | **0.8747** |
| 方向一致率 | 98.99% | 86.64% | **95.88%** |
| 增益 | 0.874 | 0.516 | **0.7186** |
| 最佳延迟 | 0.10s | 0.10s | **0.10s** |
| wz 翻转率 | 18.7/min | 20.1/min | **16.7/min** |

### 增益归因讨论（重要修正）

- ~~电池是主要混淆因素~~ → **撤回**。Jetson tegrastats 两轮对比：
  昨天低增益轮 tj 中位 58.0°C / 峰值 1984MHz，今天 60.3°C / 1984MHz，
  **Jetson 两轮均未降频**，且今天更热增益反而更好。
- 增益衡量的是**执行侧**（UpBoard RL + 电机/电调），候选因素：
  电池电压、电机/电调温度降额、地面状况、RL 状态。现有仪表无法区分。
- **用户决定不做电压/温度监控，归因挂起**。若未来要归因：把电池电压 +
  UpBoard 温度加入发车前快照（UpBoard 经 Jetson 跳板 `ssh xjzx@10.0.0.6`）。

### 新观察（不阻塞，需留意）

- `observation buffer` 过期告警 90 次（15.45/min）：R20 为 110/min（时间戳坏），
  09-04 驾驶轮为 0。文档既有的定性是良性噪音（`expected_update_rate=0.3` 偏高），
  伴随 `control_loop_missed` 6 次（1.03/min），提示带载+录制时有轻度 CPU 压力。
- FAST-LIO 队列积压（R22 根因 2）**今天未出现**，证实其为启动时序的条件性现象；
  「丢弃过期帧」加固补丁降级为待办。

### 遗留待办

1. `lddc.cpp` 主机时间戳补丁提交（本轮已提交，push 待用户批准）
2. FAST-LIO「丢弃过期帧」加固补丁（防条件性积压复发）
3. 下一步主线：2.5D 可通行性感知（开相机、启用 elevation_map/step_detector、
   标定 `step_max=0.50`）→ 接入 global_costmap + inflation 调整 → 跨楼层定位验证


---

## R24 状态更新（2026-09-07）：FastDDS SHM 静默死亡事故与 UDP-only 加固

### 事故经过（2026-09-07 11:27–11:35 全面测试）

- goal1 / goal2 **成功**（11:28:10 / 11:28:55），此期间全链路健康
- goal3（11:29:45）失败：`Failed to make progress`
- 11:30:57 起 `Robot is out of bounds` 持续刷屏，costmap 失明
- 表现为「导航栈崩溃」：`/Odometry`、`/cloud_registered`、`/cloud_registered_body`
  **Publisher count = 0**，ICP fitness **0.000**，但 `fastlio_mapping` **进程存活**（12 线程）
- 前兆：发车前 1 分钟（11:26:00）已出现首条点云过期告警

### 根因：FastDDS SHM 传输静默死亡（与 08-31「雷达掉线」不同根）

- 直接证据：`RTPS_TRANSPORT_SHM Error: open_and_lock_file failed`（SHM 端口锁损坏）
- 进程活着但 **DDS 参与者从发现列表消失** → 发布静默丢失 → costmap 失明 → 导航瘫痪
- **判别法**（建议入故障速查表）：`/livox/lidar` 年龄正常 + FAST-LIO 输出话题
  `Publisher count = 0` + 进程存活 → DDS/SHM 层故障，**不要**去查雷达
- 疑似触发：反复重启栈累积 SHM 端口文件 + 录制进程停止时的端口锁孤立

### 修复：禁用 SHM，强制 UDPv4（+8MB 收发缓冲）

部署清单：

| 项 | 位置 | 说明 |
|---|---|---|
| `fastdds_udp.xml` | `/home/nvidia/` + **仓库根**（版本控制） | UDPv4 only，`sendBufferSize/receiveBufferSize=8388608` |
| 环境变量注入 | `nav_restart.sh`（备份 `.bak_shmoff`） | `FASTRTPS_DEFAULT_PROFILES_FILE` |
| 环境变量注入 | `~/.bashrc`（备份 `.bak_shmoff`） | 同上，覆盖交互终端 |
| 内核缓冲 | 既有 `99-livox-udp.conf` | rmem_max 8MB / wmem_max 4MB（已够，无需 sudo） |

**踩坑 1**：只禁 SHM 不加缓冲时，大点云走 UDP 回环默认 208KB 套接字缓冲溢出丢包，
`/Odometry` 只收到 8%（0.8Hz）、年龄恒定 1.35s——**必须**配 8MB 收发缓冲。
**踩坑 2**：SSH 中断导致一次重启实际未执行，验证读到旧日志——验证时必须核对
HOSTSTAMP 时间戳是否新鲜。

### 验证结果（禁 SHM + 8MB 缓冲）

| 指标 | SHM 时代 | **UDP-only 现在** |
|---|---:|---:|
| `/Odometry` 中位 / P99 / max | 23-26 / 135 / 880ms | **23.6 / 28.4 / 34.9ms** |
| 超 0.1s 占比 | 0.93% | **0.00%** |
| 漂移 | −1.6 s/h | **+0.012 s/h** |
| `/cloud_registered` | 10Hz | **9.98Hz** |

### 跨机通信影响评估

**无影响**。SHM 仅作用于同机进程间；RViz2 远程订阅/发 goal、domain_bridge 多机
协同走的都是 UDP over WiFi，保持不变。本方案消除的是「同机数据被 SHM 静默吞掉」。

### 遗留

1. 09-07 全面测试未完成（goal3 后瘫痪），需重跑
2. 09-07 bag（`20260907_nav_fulltest`，226MB）保留作事故证据，含 goal1/2 成功段


---

## R25 状态更新（2026-09-07）：2D 导航封板

### 封板声明

**范围**：FAST-LIO 定位 → Nav2 规划控制（Navfn + DWB + voxel 层）→ bridge/TCP → UpBoard RL 的平地导航链路。
**依据**：R21 满电复测（7/9，78%）+ R24 加固后全面测试（8 目标全程零软件故障）两轮实车。
**判定**：修改前问题（过期+越界速度喂 DWB、SHM 静默死亡）已消除且经带载验证；规划、避障、重规划实测可用。**2D 平地导航视为稳定版本，后续改动需走回归。**

### 冻结基线（回归验收用）

| 指标 | 冻结值（带载+录制实测） |
|---|---|
| `/Odometry` 年龄 | 中位 ~24ms，max <100ms（75s 窗内超 0.1s 占比 0%） |
| `/nav_odom` 年龄 / `vy` | ~0.5ms / 恒 0 |
| `No valid trajectories` / `patience` / `oob` / `extrapolation` / abort | 全 0 |
| DWB 输入任一越界 | 0.000% |
| `control_loop_missed` | <1.1/min |
| 全局规划 | ~0.84Hz 连续无失败；局部规划 ~8.5Hz |
| 转向链 lag / 方向一致率 | 0.10s / 90.6~99%（增益见已知限制） |
| 成功率（随机目标） | 50~78%（随执行侧状态波动） |

### 封板时固化的三项代码改动（本日提交）

| 提交 | 内容 |
|---|---|
| `2d35892` | livox 驱动主机时间戳补丁（设备钟漂移根治，含 HOSTSTAMP 诊断） |
| `9fe4191` | FAST-LIO sync_packages 丢弃过期帧（防条件性积压，max 880ms→41.5ms） |
| `c16f4bb` | R24 文档 + `fastdds_udp.xml` 入库（SHM→UDPv4 only + 8MB 缓冲） |

仓库外部署（已在生效，无版本控制，记录在案）：`nav_restart.sh` 与 `~/.bashrc` 注入
`FASTRTPS_DEFAULT_PROFILES_FILE`（备份 `*.bak_shmoff`）；发车门禁脚本
`verify_preflight.sh` 等位于 `/home/nvidia/`（本地镜像 `ji/work/remote_scripts/`）。

### 已知限制（封板范围内接受）

1. **执行侧转向增益随运行状态波动**（实测 0.45~0.87）：电量/电机温度因素，
   归因挂起（已决定不做电压/温度监控）。表现为大转角目标偶发
   `Failed to make progress`（最高 1.43/min），软件链路可自恢复，未见连锁故障。
2. 失同步仍不自愈（历史已知）；雷达断电 10s 重启流程保留。
3. `FastDDS UDP-only` 依赖 8MB 缓冲配置，**`fastdds_udp.xml` 丢失或环境变量未注入
   会在同机通信上退化为高丢包**（症状：`/Odometry` 只到 ~1Hz、年龄恒定 1.35s）。

### 验证过的故障修复（本次封板前）

| 故障 | 根因 | 修复 |
|---|---|---|
| `/Odometry` 老化 2.28s + 反复 loop back | Livox 设备钟落后 3s 且漂移 6~15s/h | 主机时间戳补丁 |
| 「导航栈崩溃」（进程活但输出断流） | FastDDS SHM 端口锁损坏 | 禁 SHM + 8MB 缓冲 |
| 条件性 1.45s 稳态积压 | sync_packages 永不排空队列 | 丢弃过期帧 |

### 下一阶段（未封板）：2.5D 可通行性感知

开 `enable_camera:=true` → 启用 `elevation_map`（0/80/100 分级）+ `step_detector`
→ **标定 `step_max=0.50`** → 可通行性接入 `global_costmap`（inflation 需同步调整，
否则 80 代价区仍被膨胀成 lethal）→ 粗糙地形放宽 progress checker → 楼梯间 3D
PCD 地图 + 跨层 ICP 验证。

### 待办

1. ~~**push 5 个提交**（2d35892 / 53ac462 / 9fe4191 / c16f4bb / 本提交），等用户批准~~
   ✅ **已完成** ←订正2026-09-11：经狗上核实，`git log origin/main..HEAD` 为空、`git status` 干净，
   全部提交已推送；狗已具备直连 GitHub 推送能力（详见本节上文推送通道）。
2. 执行侧增益：下次满电复测取一组对照数据（不装传感器，仅记录）


---

## ★ 09-16 补充：DDS 接口白名单（多网卡 locator 冗余）

**改动**：`~/fastdds_udp.xml` 增加 **`interfaceWhiteList = 192.168.31.91`**（仅 WiFi 接口）。

**为什么**：狗有 **3 张网卡**——`wlP1p1s0`(192.168.31.91) / `enP8p1s0`(10.0.0.48，UpBoard 直连) / `enx00e04c680779`(192.168.1.100，Mid-360 雷达直连)。无白名单时 FastDDS 会把**三个地址全部宣告**给对端，主车据此向后两个**跨机不可达**地址持续发送数据——实测 **约 70 pps** 无效发送（占主车 TX 的 1/3），白烧 WiFi 空口。

**加白名单后**：主车出包中 `192.168.1.100` 归零、`10.0.0.48` 降 85%，无效流量整体 ~97 pps → ~27 pps。

**⚠️ 注意**：
- **狗的 DDS 不配 peers**（靠主车主动单播发现来找它）→ 因此**主车侧 peers 若丢失或写错，狗会连带失联**（robot1 因两端都有 peers 更稳）。
- 改完需重启才生效：`systemctl --user restart robot2-adapter && bash ~/nav_restart.sh`
- 完整配置、校验和与回滚见《01_系统与车辆档案.md》§8 网络与DDS配置基线。
