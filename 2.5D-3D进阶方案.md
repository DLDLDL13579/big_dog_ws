# 机械狗 2.5D / 3D 进阶方案

> 2026-09-11 编制 ｜ 依据：狗板代码逐行核对（git `2bfcd8c`）+ 运维/技术文档 + 训练教程
> **2026-09-15 更新**：阶段① 配置已实机验证通过（§10.6），导航实车闭环打通；楼梯已标定、可通行区方案落地（§11）。
> 前置认知：**2D 平地导航已于 R25（09-07）封板**（9 目标 78%、越界 0、`/Odometry` 中位 24ms）。
> 本文回答：在这个已稳的地基上，怎么往 2.5D 和 3D 走，每一步做什么、卡在哪、怎么验收。

---

## 0. 结论摘要

| 阶段 | 目标 | 状态 / 工作量 | 最大障碍 | 是否阻塞后续 |
|---|---|---|---|---|
| **① 2.5D 可通行性** | 让 costmap 区分"可跨台阶"与"必须绕行" | ✅ 配置验证通过（09-15） | 绝对 z 口径使可越格恒为 0 → 改用区域白名单绕开（§11） | 是（② 的前置） |
| **② 2.5D 导航** | 坡道/缓台阶/粗糙地面自主通行 | 2–4 周 | DWB 是 2D 差速模型，坡度下预测失真 | 否，可与 ③ 并行 |
| **③ 3D 定位 / 跨楼层** | z 不再被钉死、楼梯间 ICP 不丢 | 2–4 周 | **`T_map_to_odom[2,3]=0.0` 强制归零**（代码硬约束） | 是（跨楼层前提） |
| **④ 3D 步态 / RL 地形适应** | 策略"看见"地形、能爬楼 | 6–12 周（含训练） | **当前策略 57 维盲走**；UpBoard 控制核心无源码 | 是（真 3D 核心） |

**一句话**：2.5D 是"改配置 + 标定 + 修膨胀"，性价比最高，阶段① 已于 09-15 完成配置验证并落地楼梯可通行区（§10–§11）；3D 的真正瓶颈不在 Nav2，而在**策略没有地形观测**与**控制核心是预编译黑盒**——这两条决定了 3D 只能走"分层"路线（见 §7），不能指望一次做成"3D 自主导航"。

---

## 1. 现状盘点：我们站在哪里

### 1.1 已封板的 2D 链路（可直接复用）

```
FAST-LIO2(3D 里程计) → ICP 3D 全局定位 → map→odom TF
  → pcd_to_map(3D PCD→2D /map) → Nav2(Navfn+DWB) → /cmd_vel
  → bridge(R9-R21 补偿链) → TCP 小端 → UpBoard RL 策略 → 狗体运动
```

**这条链路的每一环都已实车验证**，且有三轮实测冻结基线（R23/R24/R25）。任何进阶都**不应破坏它**——所有新增能力必须以"可开关、可回退"的方式挂上去。

### 1.2 已具备但被 2D 架构"浪费"的 3D 能力

| 资产 | 现状 | 被浪费的点 |
|---|---|---|
| FAST-LIO2 | 输出 3D 位姿 + 去畸变点云（`/cloud_registered`、`/cloud_registered_body`） | 喂给 costmap 时被投影成 2D lethal 栅格 |
| ICP 3D 全局定位 | 3D PCD 匹配，fitness 1.000 | `map→odom` 的 **z 被强制归零**（见 §5.1） |
| 3D PCD 地图 | 在用 **51973789 字节≈52MB、1624173 点**（2026-09-18 重建成图：走行约 120m 闭合回环、终点闭合 0.0m、Z 漂移 0.003m）←订正2026-09-20（旧值 21MB / 09-10 第三轮） | `pcd_to_map` 只切 z∈[0.15,1.8] 一层；2D 图随新图变为 **1380×910 @0.05m，origin (−12.812, −15.218)**，已与主车在用图同坐标系 |
| voxel_layer | 3D 体素（`z_voxels=16`, `z_resolution=0.15`, 窗口 -0.75~1.65m） | 只做"有无障碍"，不表达高度语义 |

### 1.3 已写好但被注释禁用的 2.5D 资产

| 节点 | 功能 | 禁用原因 | 状态 |
|---|---|---|---|
| `elevation_map_node.py` | PCD → `/elevation_costmap`：**0=平地 / 80=可越台阶 / 100=绕行** | CPU ~57%（09-01 注释） | 代码完好，逻辑已定稿 |
| `step_detector_node.py` | D435i 实时前方台阶 → `/step_ahead` + `/step_height` | CPU 56.9% | 代码完好，参数待标定 |

> ⚠ 二者都有一个**必须修的实现缺陷**：`elevation_map_node.py` 声明了
> `ground_thresh/h_flat/h_step/cost_step` 四个参数，但实际计算里用的是**硬编码**
> `occ_lo,occ_hi = 0.15,1.8` 和 `step_max = 0.50`（绝对 z）。
> 即"调参旋钮是假的"，标定时必须先把这个接上。

### 1.4 训练侧（比预想的强）

- IsaacLab + Docker（`isaac-lab-base`），PPO/rsl_rl，**4 个注册环境**：
  `Flat-Z1W-V2-v0` / `Rough-Z1W-V2-v0`（各 4096 envs）+ 两个 Play 版
- `rough_env_cfg.py` 已存在（Rough 地形 + 奖励覆盖），训练入口 `./train_z1w.sh rough 500`
- MuJoCo 资产：`z1w_v2_mujoco.xml` + **`scene_corridor_generated.xml`（走廊场景）**
- **但**：部署的 ONNX 策略是 **57 维、无地形输入**（R9 源码级分析确认）
  → 现役策略本质是**盲走**（proprioception-only），Rough 训练的模型要么没用上、要么训练时也没给 height_scan

---

## 2. 技术本质：Nav2 的信息瓶颈在哪

理解这一点，才知道 2.5D/3D 该改哪里。

**Nav2 是 2D 导航栈**——`costmap_2d` 的数据结构就是一张 2D 栅格（`OccupancyGrid`），
规划器（Navfn/SMAC）与控制器（DWB/MPPI）都在 2D 平面里工作。所谓"3D 导航"只有两条路：

- **路 A（2.5D）**：把 3D 信息**压缩成 2D 栅格上的语义代价**，继续用 Nav2
- **路 B（真 3D）**：绕开 Nav2 用 3D 规划器/专用栈（成本极高，见 §7）

我们走 A，而现在的问题是**压缩过程中把所有高度语义都丢成了一个值**：

```
现在：z∈[0.15,1.8] 有点 → lethal(100)          ← 台阶、墙、椅子，全是 100
2.5D：z∈[0.15,1.8] 有点 → 按"最高点"分级
        < 0.50m → 80（可越，规划器可穿但代价高）
        ≥ 0.50m → 100（绕行）
```

**这就是 `elevation_map_node.py` 已经写好、但没接进链路的东西。**

有三个"2D 化损失点"需要意识到：

| 损失点 | 位置 | 损失了什么 |
|---|---|---|
| PCD → 2D /map | `pcd_to_map_node.py`（固定 z 窗 0.15~1.8） | 建图时的高度信息（一次性丢失） |
| 点云 → voxel → 2D | `nav2_3d_params.yaml` voxel_layer | 实时 3D 被压成"有/无" |
| 2D 规划 | Navfn（`allow_unknown=true`, `tolerance=0.5`） | 规划器不理解"越障"，只会绕 |

---

## 3. 阶段①：2.5D 可通行性（1–2 周，建议立刻做）

### 3.1 目标

在**不破坏已封板链路**的前提下，让全局代价地图区分"可越"与"绕行"，并让狗真的
**选择跨过一个小台阶而不是绕路**。

### 3.2 具体动作（代码级）

| # | 动作 | 文件/位置 | 说明 |
|---|---|---|---|
| 1 | 修 `elevation_map_node.py` 的假参数 | `scripts/elevation_map_node.py` L29-34 / L62-63 | 让 `occ_lo/occ_hi/step_max` 真正从参数读取，便于标定 |
| 2 | **标定 `step_max`** | 同上（参数化后） | 用 `step_detector` + 实车，测出**狗靠现有盲走策略能可靠跨过的最大高度** |
| 3 | 启用 D435i + 两节点 | `navigation_launch.py`（取消注释两处） | 需要 `enable_camera:=true` |
| 4 | **解决 inflation 冲突**（最大工程点） | `nav2_3d_params.yaml` | 见 §3.3 |
| 5 | 高程图接入 global_costmap | `nav2_3d_params.yaml` | 方案见 §3.3 |
| 6 | 局部代价地图保持不动 | — | `voxel_layer` 继续负责**实时**避障（Livox 5m 内） |

### 3.3 三个必须解决的冲突

**冲突 1：`inflation_radius=0.55` 会把 cost 80 的区域膨胀成 lethal**

膨胀层是**按距离无条件加代价**的——可越台阶周围 0.55m 内都会被推高到 lethal，
结果"能跨的地方"在规划器眼里和墙一样。可选解法（按侵入性排序）：

- **a. 给台阶区单独降膨胀**：把可越区从膨胀层的输入里排除（需要自定义 layer 或对
  `/elevation_costmap` 做后处理，把 80 区域的膨胀限制在小半径）
- **b. 只让 Navfn 用高程图，不让膨胀层吃它**：把 `/elevation_costmap` 作为
  **planner 专用代价层**（`global_costmap` 里加一个自定义 `StaticLayer` 指向它，
  且 `combination_method=0` 取最大值而非覆盖），膨胀只作用于 voxel 的 lethal
- **c. 降低 global 的 `inflation_radius`**：简单粗暴，但会牺牲平地安全余量 —— **不推荐**

**冲突 2：静态高程图 vs 建图遮挡**

`elevation_map` 基于**静态 PCD**，而建图时家具正下方的地面被遮挡扫不到（Livox 视角限制）。
已有定稿对策（见技术文档）：确定性地面基准（z 的 **3% 分位**）+ 与占用判据对齐
（z∈[0.15,1.8] 有点=占用）+ 占用格内按最高点分级。**先按此实现，暂不上实时高程图。**

**冲突 3：CPU 预算**

两节点合计约 114% CPU，而当前导航链路本来就紧（R25 基线 `control_loop_missed <1.1/min`）。
对策：
- `elevation_map` 是**一次性计算 + 3s 重发**，可降到"只在启动时算一次"（静态图无需周期重发）
- `step_detector` 已优化过（OPENBLAS=1 线程 + 10Hz），保留
- 上之前先跑 `verify_preflight.sh` 拿 CPU 基线，**一次只加一个节点**

### 3.4 验收标准

1. 在有一段 8–15cm 台阶的场景，发目标到台阶另一侧 → 狗**跨过去**而不是绕行/卡住
2. 同一场景把台阶换成同高度的实心箱体（`step_max` 之上）→ 狗**绕行**
3. 平地回归测试（R25 同款 9 随机目标）成功率**不低于 70%**
4. `control_loop_missed` 不明显恶化（<2/min）

---

## 4. 阶段②：2.5D 导航（2–4 周，可与③并行）

阶段①解决了"能不能走"，阶段②解决"走得好不好"。

| # | 动作 | 依据 | 说明 |
|---|---|---|---|
| 1 | **放宽 progress checker** | R20 只加了 `required_movement_angle=0.15` | 粗糙地形上速度慢，`movement_time_allowance=10s` 会误判"无进展" |
| 2 | **地形自适应限速** | 当前 `max_vel_x=0.45` 是平地调优值 | 按可通行性代价/坡度分档降速（如 cost 80 区域限 0.25） |
| 3 | **控制器评估** | DWB 是 2D 差速模型 | 坡道上 DWB 的轨迹预测会失真；Nav2 有 **MPPI**（支持更复杂模型），可作为 B 方案对比 |
| 4 | **z 轴与姿态** | — | 2.5D 地形上 `base_link` 的 z/pitch 会真实变化，需确认 ICP 的 z 归零策略（见 §5.1）不与之冲突 |

---

## 5. 阶段③：3D 定位与跨楼层（2–4 周，跨楼层的**硬前提**）

### 5.1 第一阻塞：z 被强制归零

```python
# fast_lio_localization/scripts/global_localization.py
L178:  self.T_map_to_odom[2, 3] = 0.0   # z 归零
L203:  self.T_map_to_odom[2, 3] = 0.0   # ★ CatPaw: 强制 z 归零，避免 ICP 漂到地下
```

这是**为平地设计的防漂移措施**（原始问题：ICP z 漂到地下 → `sensor out of bounds` 1013 次）。
但跨楼层时它会**主动抹掉真实的高度变化**——狗上到二楼，`map→odom` 的 z 仍是 0。

**解法方向**（不建议直接删除该行）：
- **条件化**：平地模式下保持归零；检测到"进入楼梯区"时放开 z，并收紧 ICP 的
  z 搜索范围（防漂）而非一刀切归零
- **触发信号**：可由 `step_detector`/高程图给出"地形模式"标志，或用人工确认切换
- **验证**：先在**缓坡**上验证（z 缓慢变化）→ 再上楼梯（z 阶跃变化）

### 5.2 第二阻塞：楼梯间的 ICP 几何退化

楼梯间是**周期性重复几何**（每级台阶一模一样），ICP 极易误匹配到相邻的级/层。

对策：
- **多尺度 + 强初值**：靠 FAST-LIO 的连续里程计做初值（短时精度高），ICP 只做小修正
- **降 ICP 权重 / 提阈值**：在楼梯区让 `localization_th` 更严，宁可"不修正"也不要"错误修正"
- **辅助约束**：楼梯间可用 IMU 的姿态（pitch 恒定）作为强先验

### 5.3 地图策略

- 建图阶段把楼梯走一遍（同一个 PCD 含多层）
- `pcd_to_map` 当前只切 z∈[0.15,1.8] 一层 → **跨楼层需要按 z 分带出多张 2D 图**，
  或用"当前楼层"标志动态切换 static_layer 的输入
- 楼层判定：由 z 的绝对值区间 + 人工锚点

---

## 6. 阶段④：3D 步态与 RL 地形感知（6–12 周，真 3D 的核心）

### 6.1 缺口：现役策略是"盲走"

R9 的源码级分析已定案：ONNX 策略观测 **57 维 = 3 角速度 + 3 重力 + 3 命令 + 16 q + 16 dq + 16 上帧动作**，
**没有任何地形/高度输入**。它能走平地、能被轮腿结构"被动"吸收小起伏，但**爬楼必须换策略**。

### 6.2 两条升级路线

| 路线 | 做法 | 优点 | 代价 |
|---|---|---|---|
| **A. 本体感知（proprioception）** | 不加传感器，靠关节/IMU 历史隐式推断地形（ANYmal 路线） | 不动硬件、不动外参 | 需要**长时序**网络（当前是 LSTM，方向对）；成功率低于视觉；训练量大 |
| **B. 外部高程图输入** | 把局部高程图（如 17×11 的 height scan）加进观测 → 观测维度 57→~250 | 成功率最高，可"看见"再走 | 需要**外参精确 + 时间同步**（我们已有亚毫秒时间戳基础）；UpBoard 接口必改 |

**建议：走 B，但用 A 兜底**。我们的时间戳同步（R22 主机时间戳补丁，`/livox/lidar` 10ms）
已经是做视觉-本体融合的前提条件，这是别人没有的优势。

### 6.3 ★ 最大风险：UpBoard 控制核心是**预编译黑盒**

这是必须摆在台面上的问题：

- 推理侧的逻辑（`FSM_State_RL.cpp`、`RLConfig.h`、观测拼装）**不在发布包里**——
  `robot-software/build/libbiomimetics.so` 是 126MB 预编译二进制（厂家提供）
- 我们能改的只有 `robot/src`（接口层，如 `rt_rc_interface.cpp` 的 TCP 注入），
  **观测维度一旦从 57 改成 250，上游拼装代码必须跟着改** → 需要厂家支持或逆向
- 先例：TCP 注入补丁就是"绕过发布包、在 `bigdog_build` 单独编译"才做成的
  （见 `upboard补丁留档/`）——**同样的模式可以做，但技术要求高、风险大**

**因此**：4 之前必须先做一个**接口可行性确认**——查清推理入口在哪、观测拼装是否可改、
onnx 是否是"输入=观测、输出=动作"的直接映射。

### 6.4 训练与部署链路（已有基础）

```
IsaacLab(Docker) → rsl_rl/PPO → .pt → 导出 ONNX → 拷到 UpBoard/onnx/
                 ↑                                    ↓
        rough_env_cfg.py（已存在）            mit_ctrl 加载推理
```
- 训练侧只需**扩展观测**（加 height_scan）+ **建楼梯地形**（IsaacLab 有 `TerrainImporter` 的
  stairs 生成器，或用 `scene_corridor_generated.xml` 的走廊+台阶变体）
- **训练-部署一致性**是红线（R9 反复强调）：obs 顺序、归一化、缩放系数、控制频率（50Hz）必须逐项对齐

---

## 7. 架构建议：分层，而不是"一体化的 3D 导航"

不要试图让 Nav2 做 3D 路径规划（那是死路）。业界实用做法是**分层接力**：

```
┌─────────────────────────────────────────────────────────┐
│ 导航层（Nav2 / 2.5D）                                     │
│  职责：把狗从 A 送到"楼梯口"；从"楼梯出口"送到 B           │
│  能力：可通行性分级 + 平地已封板                          │
└───────────────────────┬─────────────────────────────────┘
                        │ 到达楼梯口 → 触发"爬楼模式"
                        ▼
┌─────────────────────────────────────────────────────────┐
│ 步态层（RL 策略接管）                                     │
│  职责：自主爬完这一段楼梯（视觉/本体感知驱动）             │
│  前提：策略有地形观测（§6.2）+ 观测接口可改（§6.3）        │
└───────────────────────┬─────────────────────────────────┘
                        │ 到达楼梯出口 → 交还导航层
                        ▼
                  （导航层重新定位 + 继续）
```

**为什么这样分**：
- 楼梯段的"路径"本质上由**步态决定**（落脚点规划），不是 2D 路径的问题
- 分层后，楼梯段失败**不会污染**平地导航（可回退、可人工接管）
- 安全边界清晰：楼梯口是天然的"交接点"，人可以在那里确认

**需要的胶水**：一个"任务状态机"（平地导航 ↔ 爬楼模式切换），以及**交接点的定位锚点**
（"楼梯口"在 map 系里的坐标 + 朝向）。

---

## 8. 风险清单与前置条件

| # | 风险 | 影响 | 缓解 |
|---|---|---|---|
| 1 | **UpBoard 控制核心无源码** | §6 的观测改造可能做不了 | 先做接口可行性确认；不行则退到本体感知路线（A） |
| 2 | **z 归零 vs 跨楼层** | 跨楼层定位直接失效 | 条件化而非删除；先前述缓坡验证 |
| 3 | **inflation 吃掉可越区** | 2.5D 做了等于没做 | §3.3 方案 a/b |
| 4 | **楼梯间 ICP 退化** | 定位丢失 | 强初值 + 严阈值 + IMU 先验 |
| 5 | **CPU 预算** | 拖垮已封板的控制实时性 | 一次只加一个节点；静态高程图不周期重算 |
| 6 | **楼梯安全** | 摔狗/伤人 | 安全绳 + 人盯 + 遥控器在手；先在仿真过 100% 再上实车 |

---

## 9. 建议的推进顺序

```
第 1–2 周：① 2.5D 可通行性
  ├─ 修 elevation_map 假参数 → 标定 step_max
  ├─ 解决 inflation 冲突（方案 b 优先）
  └─ 验收：跨 8–15cm 台阶 / 绕 15cm+ 箱体 / 平地回归 ≥70%

第 3–6 周：② 2.5D 导航  ∥  ③ 3D 定位（并行）
  ├─ ② progress checker / 地形限速 / MPPI 评估
  └─ ③ z 归零条件化 → 缓坡验证 → 楼梯间 ICP 加固 → 多楼层地图

第 4 周（穿插）：④ 的可行性确认
  └─ 查清 UpBoard 推理入口与观测拼装是否可改  ← 决定 ④ 走 A 还是 B

第 7 周起：④ 3D 步态（最大工作量）
  ├─ 仿真：扩展观测 + 楼梯地形 → 训练 → sim2sim 验证
  ├─ 交接：任务状态机 + 楼梯口锚点
  └─ 实车：安全绳 → 单级台阶 → 缓坡 → 完整楼梯
```

**里程碑**：
- **M1（2 周）**：狗能自主跨过一个小台阶（2.5D 生效）
- **M2（6 周）**：缓坡 + 粗糙地面稳定通行（2.5D 导航完成）
- **M3（8 周）**：跨楼层定位不丢（3D 定位完成）
- **M4（12 周+）**：仿真 100% → 实车爬一段楼梯（3D 步态完成）

---

## 10. 阶段① 实施记录与测试清单（2026-09-14）

> 本节是**实施层**记录：已改了什么、离线复算发现了什么、语义如何订正、下次照着测什么。
> 与 §3 的设计论证冲突时，**以本节为准**。

### 10.1 已完成的改动（配置就位，导航栈未重启）

| # | 改动 | 文件（狗端 `~/dog_ws/src/dog_brain/`） | 说明 |
|---|---|---|---|
| 1 | global_costmap 新增 `elevation_layer` | `config/nav2_3d_params.yaml` | `StaticLayer` + `map_topic: /elevation_costmap` + `map_subscribe_transient_local: true` + `subscribe_to_updates: false` |
| 2 | 新增两个**全局**参数 | 同上 | `use_maximum: true`（叠加取最大，elevation 的 0 不会抹掉主图的墙）、`trinary_costmap: false`（依据见 §10.5） |
| 3 | 启用 `elevation_map` 节点 | `launch/navigation_launch.py` | 取消注释节点定义与 nodes 列表项；**`step_detector` 仍禁用、不开相机**（与 §3.2 第 3 项不同：`elevation_map` 基于静态 PCD 不需要相机，省 56.9% CPU） |

- 备份：`config/nav2_3d_params.yaml.bak_20260914_2p5d`、`launch/navigation_launch.py.bak_20260914_2p5d`
- 改动前 md5：yaml `ac6a1042da5a67d67a1201ad6c28689d`、launch `0cf021da8f0030b5735ad8b82086c26b`
- **导航栈未重启 ⇒ 改动尚未生效**（正在跑的栈仍用旧配置）

### 10.2 离线复算的三个发现（实测数据）

对在用图 `src/dog_brain/maps/lab_3d_map.pcd`（21MB，09-10 18:24 版）复算 `_build()` 逻辑：

> **←订正2026-09-20：本节复算针对的是 09-10 旧图（21MB）；该图已于 2026-09-18 被新图取代
> （51973789 字节≈52MB、1624173 点），因此下表各项数值与据此得出的"可越格 0 个"结论
> 均需在换图后重新复算。** 另注：新图 2D 栅格为 1380×910 @0.05m、origin (−12.812, −15.218)，
> 与本表所列 2410×992 的 elevation 栅格不是同一张图，勿混。

| 指标 | 值 |
|---|---|
| 地面基准 GZ（z 的 p3） | **-0.354 m**（地面不在 z=0） |
| 全图点数 → 去鬼影后 | 670,220 → **349,064（48% 是 z>地面+3m 的鬼影）** |
| 栅格 | 2410×992 @0.05m；有效范围 x −18.2~53.1、y −17.0~31.2 m |
| **当前参数**（occ 0.15/1.8 绝对）可越格 | **0 个** |
| **相对口径**（GZ+0.15 / GZ+1.8）可越格 | 12,560 个 |

**发现 1**：当前参数下可越格为 0 —— `occ_lo=0.15` 是绝对 z（假设地面在 0），而实测 GZ=-0.354，**占用带下界实际是"离地 0.504 m" > `step_max`(0.50)**，占用带内的点全部判障碍。

**发现 2**：改相对口径后多出的 12,560 格中，最大一片（原点周围 **9.9×8.3 m、14.5 m²、5791 格**）在绝对口径下 **100% 是"平地"** —— 是**地面基准空间偏差**造出的假可越区，不是真台阶。

**发现 3**：1m 分块地面基准的空间波动：p25=−0.478 / p50=−0.312 / p75=−0.171，**主体区间幅度约 0.31 m**，与 2.5D 要判别的特征尺度（0.06~0.50 m）**同量级**。这是"全图单一 GZ + 逐格高度分级"的精度上限。

### 10.3 语义订正（用户 2026-09-14 明确）

> **可越区域应当"低代价、优先通过"，不是"可穿但代价高、尽量绕行"。**

原配置按"80 → cost 203（接近 lethal）"设计，方向**相反**，需订正。将来地图 z 修好、可越区真正进入占用带时，应把"可越"映射成**低代价**（例如 `cost_step` 由 80 降为 3~8，经 StaticLayer 换算约 cost 8~20）。

### 10.4 场景事实与目标（用户 2026-09-14 明确）

- 地图里**几乎没有台阶**，唯一地形是**一个 2 m 长、约 1 m 高的人造楼梯**
- **遥控完全能爬** ⇒ **运动层已验证**（RL 盲走策略扛得住 1 m 楼梯）
- 目标：狗**爬上去**（不是绕开）⇒ 2.5D 的活是**让规划器允许规划路径穿越楼梯区域**

自主爬楼梯的四层状态：

| 层 | 状态 |
|---|---|
| 运动层（身体能爬） | ✅ 已验证（遥控实测） |
| **规划层**（允许路径经过楼梯） | ⬜ 2.5D 待做（楼梯现判 100 ⇒ 规划器绕行） |
| 控制层（楼梯上别乱冲） | ⬜ 待做（DWB 用平地参数，需地形限速，见 §4） |
| 定位层（楼梯上 z 失真） | ⬜ 待实测（`T_map_to_odom[2,3]=0` 强制 z 归零） |

### 10.5 Nav2 源码关键事实（配置依据，已逐条查证）

1. **`StaticLayer::interpretValue`**（`nav2_costmap_2d/plugins/static_layer.cpp`）：`value >= lethal_threshold_(100)` → LETHAL；否则若 `trinary_costmap_`（**默认 true**）→ **FREE_SPACE**；否则 `value/100*254`。⇒ **默认配置下 80 被抹成 FREE(0)，2.5D 完全失效**；必须 `trinary_costmap: false` 才走 `80/100*254 = 203`。
2. **`InflationLayer` 只膨胀 `LETHAL_OBSTACLE(254)`**（`inflation_layer.cpp` L237）⇒ 203 **不会被膨胀**，§3.3"冲突 1"在 203 路径下不成立。
3. **`CostmapLayer::updateWithMax`** 对 master=NO_INFORMATION 有专门分支（`old_cost == NO_INFORMATION || old_cost < costmap_[it]`）⇒ `use_maximum: true` 安全。
4. **参数解析分两套**：`map_topic`、`map_subscribe_transient_local`、`enabled`、`subscribe_to_updates` 读**带实例前缀**；而 `trinary_costmap`、`track_unknown_space`、`lethal_cost_threshold`、`unknown_cost_value`、`use_maximum`、`transform_tolerance` 读**全局**（两实例**共享**）——写成 `elevation_layer.trinary_costmap` 无效。
5. `elevation_map_node.py` 的 QoS 已是 **TRANSIENT_LOCAL**、启动只算一次并 latched 发布 ⇒ 稳态 CPU 近 0，与 §3.3"冲突 3"的处置一致。

### 10.6 测试清单执行结果（2026-09-15 实测，取代原清单）

| # | 检查项 | 结果 |
|---|---|---|
| 1 | CPU 基线 | 空载 load 0.31 → 满栈 4.6 / 8 核 |
| 2 | 狗在地图范围内 | map 系位姿 (−0.42, 0.58)，落在地图范围内 ✔ |
| 3 | `elevation_map` 启动日志 | `Ground base z=-0.354`、`2410×992`、`flat=2375162 step=0 obstacle=15558` ✔ |
| 4 | **与 §10.2 离线复算一致性** | 地面基准 −0.354 / 点数 670,220 / 栅格 2410×992 / 可越格 0 —— **四项完全吻合** ✔ |
| 5 | `/elevation_costmap` 链路 | publisher=elevation_map，QoS RELIABLE+TRANSIENT_LOCAL，subscriber=global_costmap ✔ |
| 6 | 栅格元信息 | `2410×992`、`res 0.05`、`origin (−67.35, −18.44)` ✔ |
| 7 | data 取值 | 只有 0 与 100，可越格=0 ✔ 符合预期 |
| 8 | global_costmap 主图墙体 | 服务查询 `lethal(254)=16007`、`内切(253)=94018` —— `use_maximum` 生效，墙未丢 ✔ |
| 9 | 回归指标 | `notSynced=0`、告警全 0、`/Odometry` 中位 43.6 ms、`/nav_odom` 1.6 ms ✔ |
| 10 | **导航实车** | **SUCCEEDED**，实走 3.27 m（(−1.14,0.64) → (0.71,−2.06)），`/cmd_vel` 461 条正常 ✔ |

**结论：阶段① 的配置改动已生效且健康，未破坏 R25 已封版链路。**

### 10.7 三个必须记住的坑（2026-09-15 实测踩到）

**坑 1：初始位姿时序竞态 → ICP 全局定位永不启动**

`global_localization.py::cb_init_pose` 要求 `global_map` / `cur_scan` / `cur_odom` 三者就绪，否则**直接丢弃 `/initialpose` 且不重试**；而 `/initialpose` 订阅是 VOLATILE，`publish_initial_pose.py` 在启动早期发完即退出。
实测：初始位姿比地图到达早约 48 s → 被丢弃 → `map→odom` 恒为单位变换、ICP fitness 为空 → 机器人在 map 系的位姿其实是 FAST-LIO 里程计零点漂移值（实测已漂 8 m）。

> **修复：导航栈起好后手动补发一次 `/initialpose`**（此时三条件已满足），ICP 立刻锁定、fitness 1.000。**每次重启导航栈都必须做这一步。**

**坑 2：costmap 话题拿到的是陈旧帧**

`always_send_full_costmap: false`（09-14 为缓解跨机队头阻塞改的）之后，`/global_costmap/costmap` 只在必要时发布。订阅该话题拿到的帧可能停留在"静态层写入前"，实测出现过 `254=0` 的假象，几乎被误判成"主图墙体丢失"。

> **查真实代价必须用 `/global_costmap/get_costmap` 服务**，不要用话题订阅或 RViz 直接下结论。

**坑 3：狗的停位决定导航成败**

狗离障碍小于 `inflation_radius=0.55 m` 时，footprint 会落进膨胀产生的 lethal(253) 区 → 规划器认为"起点在墙里" → 一条路径都算不出来；`clear_entirely_global_costmap` 后立刻复现，因为诊断层会重新标记。
实测：停在 (−1.44, 0.82) 时脚下 69% 是 lethal → ABORTED；挪到 (−1.14, 0.64) 后**同一目标一次通过**。

> **发车前提：把狗停在离障碍 0.6 m 以上的开阔处。**

### 10.8 未决事项（2026-09-15 更新）

- ~~楼梯在地图里的位置尚未定位~~ → **已定位**，见 §11.1
- ~~规划层方案 A / B 未选~~ → **选定方案 A**（区域白名单），见 §11
- **控制层**（地形限速）、**定位层**（z 归零条件化）未动 —— 这是"狗真能爬上去"的后续瓶颈
- 地图质量：48% 鬼影点 + 地面基准波动 0.31 m ⇒ 精细逐格分级本就吃力；方案 A 用区域标记绕开了这个弱点

---

## 11. 阶段① 收尾：楼梯可通行区标定（2026-09-15）

### 11.1 楼梯标定结果

在点云里按"离地 0.25~1.6 m"筛点、按 0.25 m 网格统计，得到楼梯实测范围：

| 项 | 值 |
|---|---|
| 平面范围 | x ∈ [1.70, 3.00]、y ∈ [−0.65, 1.55] |
| 尺寸 | 约 1.2 × 2.1 m（与用户口径"2 m 长、1 m 高"吻合） |
| 高度（相对地面 GZ=−0.354） | p25=0.52 m、p50=0.73 m、p75=1.10 m、最高 ≥1.60 m |
| 密集特征 | x≈1.75 与 x≈2.75 两条竖直带 + y≈1.2~1.45 水平带（台阶立面/踏面） |

> ⚠ 地图 `/map` 上只看得到楼梯的**上半截**（块 B，1.35×1.45 m）：`pcd_to_map` 的 `z_min=0.15` 是**绝对 z**，等效于只统计"离地 ≥0.504 m"的点。

### 11.2 为什么两个节点必须一起改（关键洞察）

只把 `elevation_layer` 改成低代价是**无效**的：

```
地面 GZ=−0.354、z_min=0.15（绝对） → 主图 /map 在楼梯区判 occupied(100)
  → StaticLayer 转成 LETHAL(254)
  → global_costmap 设了 use_maximum: true（各层取最大值）
  → max(主图 254, elevation 低代价) = 254     ← 主图赢，路径照样绕行
```

### 11.3 实施内容（方案 A：区域白名单）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `fast_lio_localization/scripts/elevation_map_node.py` | 新增 `passable_regions`、`cost_passable`；分级之后把 bbox 内格子**强制**写为低代价 |
| 2 | `fast_lio_localization/scripts/pcd_to_map_node.py` | 新增同名 `passable_regions`；占用判定后把 bbox 内格子**强制置回空闲(0)** |
| 3 | `dog_brain/launch/navigation_launch.py` | 定义 `PASSABLE_REGIONS = '1.70,-0.65,3.00,1.55'`，**同时**传给上面两个节点 |

- 参数格式 `'x0,y0,x1,y1[;x0,y0,x1,y1...]'`（世界坐标，map 系，支持多区域）
- `cost_passable` 默认 5（低代价优先通过，符合 §10.3 语义订正）
- **未改动**：`nav2_3d_params.yaml`（`cost_step` 当前不起作用，留待修口径时再动）、`local_costmap`、voxel_layer
- 备份（`*.bak_20260915_2p5dp`）：
  - `elevation_map_node.py` md5 `8254334e498dbf1c8c0b2c822177d8c7`
  - `pcd_to_map_node.py` md5 `8ea9042c03e238b4659274848e879486`
  - `navigation_launch.py` md5 `5ec2d0dd5584b53fc17568a89e8e8f54`

**状态（2026-09-15 收尾）**：代码已在 Mac 工作区写好并通过语法检查，因设备无线链路中断**尚未上机**。上机后需重启导航栈，并用 `ComputePathToPose` **只算路径不发车**确认路径穿越楼梯区。

### 11.4 风险与后续

1. **规划通了 ≠ 走得上去**：DWB 是 2D 差速模型，台阶/坡道上轨迹预测失真（§4），可能报 `No valid trajectories`。
2. **定位层隐患**：`T_map_to_odom[2,3]=0` 强制 z 归零（§5.1），狗爬上去后地图系高度仍为 0。
3. **`/map` 会在 bbox 内变 free**：该图经 `robot2_adapter` 上行给主车（显示层面）；bbox 一旦覆盖真墙，那段墙会从主图消失 —— **改完必须先核对**。
4. **验收**：跨过 8~15 cm 台阶 / 绕过 15 cm+ 箱体 / 平地回归成功率不低于 70%。

---

## 附：与现有文档的衔接

- 本文是**新方案**，不替代任何现有文档；实施时按"一次一个变量 + 备份 + 实车验收"推进
- 每完成一项，据实更新《运维与状态文档》的轮次记录（R27 起）
- 2.5D 相关的代码事实以 `elevation_map_node.py` / `step_detector_node.py` 当前实现为准
- 安全红线不变：**动狗/发车必须用户当轮明确批准**
